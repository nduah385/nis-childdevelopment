import { createClient, SupabaseAuthAdapter as NeonAuthCompatibilityAdapter } from 'https://cdn.jsdelivr.net/npm/@neondatabase/neon-js@0.7.0-beta/+esm';
(()=>{'use strict';
const $=(id)=>document.getElementById(id), cfg=window.NIS_CONFIG||{};
let neonClient=null, siteSettings={}, schoolProfile=null, albumTimers=new Map(), albumObservers=[], heroTimer=null, heroSlides=[], currentUser=null, currentAdmin=null, currentModule='overview', queryFailures=[];
const escapeHTML=(v)=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const text=(id,v)=>{const el=$(id);if(el&&v!==undefined&&v!==null&&String(v).trim()!=='')el.textContent=v};
const safeUrl=(u,allowMap=false)=>{const raw=String(u||'').trim();if(/^data:image\/(?:jpeg|png|webp|gif);base64,[a-z0-9+/=]+$/i.test(raw)||/^data:application\/pdf;base64,[a-z0-9+/=]+$/i.test(raw))return raw;try{const x=new URL(raw,location.href);if(!['http:','https:'].includes(x.protocol))return'';if(allowMap&&!/^(www\.)?(google\.[a-z.]+|maps\.google\.[a-z.]+)$/i.test(x.hostname))return'';return x.href}catch{return''}};
const icons=['🎓','🛡️','❤','🌱','🤝','💡','🏡','⚽'];
function initNeon(){
 if(!cfg.neonAuthUrl||!cfg.neonDataApiUrl||String(cfg.neonAuthUrl).startsWith('__')||String(cfg.neonDataApiUrl).startsWith('__'))return;
 try{neonClient=createClient({auth:{adapter:NeonAuthCompatibilityAdapter(),url:cfg.neonAuthUrl,allowAnonymous:true},dataApi:{url:cfg.neonDataApiUrl}})}catch(e){console.error('Neon init failed',e)}
}
async function q(table,select='*',builder){if(!neonClient)return[];let x=neonClient.from(table).select(select);if(builder)x=builder(x);const {data,error}=await x;if(error){queryFailures.push({table,message:error.message});console.error('Database query failed:',table,error);return[]}return data||[]}
async function loadSettings(){const rows=await q('website_settings','key,value');siteSettings=Object.fromEntries(rows.map(r=>[r.key,r.value]));renderSettings()} async function loadSchoolProfile(){const rows=await q('school_profile','*',x=>x.limit(1));schoolProfile=rows[0]||null;renderSettings()}
function renderSettings(){const get=(k,d='')=>siteSettings[k]??d;text('brandName',get('organisation_name','NIS Child Development Centre'));text('heroEyebrow',get('hero_eyebrow','One Organisation. Multiple Pathways. One Goal.'));text('heroTitle',get('hero_title','Nurturing Children. Empowering Communities. Transforming Futures.'));text('heroText',get('hero_text','NIS Child Development Centre advances holistic child development through education, health, protection, empowerment, family support and community development.'));text('aboutIntro',get('about_intro'));text('missionText',get('mission'));text('visionText',get('vision'));text('purposeText',get('purpose'));text('valuesText',get('values'));['missionText','visionText','purposeText','valuesText'].forEach(id=>{const el=$(id),card=el?.closest('article');if(card)card.hidden=!String(el.textContent||'').trim()});text('schoolDescription',schoolProfile?.description||get('school_description'));text('relationshipText',schoolProfile?.relationship_description||get('school_relationship','Nipe International School is the educational institution of NIS Child Development Centre.'));const schoolUrl=safeUrl(schoolProfile?.website_url||get('school_website')||'https://www.nipeinternationalschool.org/');const admissionsUrl=safeUrl(schoolProfile?.admissions_url||schoolUrl||'https://www.nipeinternationalschool.org/');['schoolWebsiteButton','footerSchoolLink','schoolWebsiteHero','schoolDiscoverButton'].forEach(id=>{const el=$(id);if(el){el.href=schoolUrl||'https://www.nipeinternationalschool.org/';el.target='_blank';el.rel='noopener'}});const admissionsButton=$('schoolAdmissionsButton');if(admissionsButton){admissionsButton.href=admissionsUrl||schoolUrl||'https://www.nipeinternationalschool.org/';admissionsButton.target='_blank';admissionsButton.rel='noopener'};const logo=safeUrl(get('organisation_logo'));if($('brandLogo')&&$('brandMark')){if(logo){$('brandLogo').src=logo;$('brandLogo').alt=get('organisation_name','NIS Child Development Centre')+' logo';$('brandLogo').hidden=false;$('brandMark').hidden=true}else{$('brandLogo').hidden=true;$('brandMark').hidden=false}}$('heroMedia').classList.remove('has-image');$('heroMedia').style.backgroundImage='';const schoolLogo=safeUrl(schoolProfile?.logo_url||'');['schoolLogo','schoolHeroLogo'].forEach(id=>{const el=$(id);if(!el)return;if(schoolLogo){el.src=schoolLogo;el.hidden=false}else{el.hidden=true}});const schoolImage=safeUrl(get('school_feature_image'));if($('schoolFeatureImage')){if(schoolImage){$('schoolFeatureImage').src=schoolImage;$('schoolFeatureImage').hidden=false;$('schoolVisualPlaceholder').hidden=true;$('schoolFeatureImage').parentElement.classList.add('has-image')}else{$('schoolFeatureImage').hidden=true;$('schoolVisualPlaceholder').hidden=false;$('schoolFeatureImage').parentElement.classList.remove('has-image')}}const map=safeUrl(get('map_embed_url'),true);if(map){$('contactMap').src=map;$('contactMap').hidden=false}else{$('contactMap').removeAttribute('src');$('contactMap').hidden=true}const bits=[['Address',get('address')],['Phone',get('phone')],['Email',get('email')],['WhatsApp',get('whatsapp')]].filter(x=>x[1]);$('contactDetails').innerHTML=bits.length?bits.map(([a,b])=>`<p><strong>${a}:</strong> ${a==='Email'?`<a href="mailto:${escapeHTML(b)}">${escapeHTML(b)}</a>`:escapeHTML(b)}</p>`).join(''):'';const instructions=String(get('donation_instructions','')||'').trim();const instructionPanel=$('publicDonationInstructions'),instructionText=$('publicDonationInstructionsText');if(instructionPanel&&instructionText){instructionText.textContent=instructions;instructionPanel.hidden=!instructions}const favicon=$('siteFavicon'),appleIcon=$('appleTouchIcon');if(logo){if(favicon){favicon.href=logo;favicon.type=''}if(appleIcon)appleIcon.href=logo;}}
async function loadInterventions(){const rows=await q('intervention_areas','id,title,summary,sort_order',x=>x.eq('is_published',true).order('sort_order'));const fallback=['Education','Child Protection','Health and Nutrition','Scholarships','Youth Development','Community Development'];const data=rows.length?rows:fallback.map((title,i)=>({title,summary:'',sort_order:i}));$('interventionGrid').innerHTML=data.map((r,i)=>`<article class="info-card"><div class="icon">${icons[i%icons.length]}</div><h3>${escapeHTML(r.title)}</h3><p>${escapeHTML(r.summary||'')}</p></article>`).join('')}
async function loadImpact(){const rows=await q('impact_statistics','label,value,year,sort_order',x=>x.eq('is_published',true).order('sort_order'));const top=rows.slice(0,4);if(top.length)$('impactStats').innerHTML=top.map(r=>`<div class="stat"><strong>${escapeHTML(r.value)}</strong><span>${escapeHTML(r.label)}</span></div>`).join('');$('impactCards').innerHTML=rows.length?rows.map(r=>`<div class="impact-card"><strong>${escapeHTML(r.value)}</strong><span>${escapeHTML(r.label)}${r.year?` • ${escapeHTML(r.year)}`:''}</span></div>`).join(''):'<div class="empty-state">Published impact statistics will appear here.</div>'}
async function loadFeed(type='programs'){const map={programs:['programs','title,summary,status,organisation_type,slug,cover_image_url','is_published'],projects:['projects','title,summary,status,location,slug,cover_image_url','is_published'],campaigns:['campaigns','title,description,status,goal_amount,amount_raised,slug,cover_image_url','is_published']};const [table,sel,pub]=map[type];const rows=await q(table,sel,x=>x.eq(pub,true).order('created_at',{ascending:false}).limit(9));$('programFeed').innerHTML=rows.length?rows.map(r=>`<article class="content-card">${safeUrl(r.cover_image_url)?`<img loading="lazy" src="${escapeHTML(safeUrl(r.cover_image_url))}" alt="${escapeHTML(r.title)}" style="width:100%;height:180px;object-fit:cover;border-radius:12px;margin-bottom:14px">`:''}<span class="meta">${escapeHTML(r.organisation_type||r.status||type)}</span><h3>${escapeHTML(r.title)}</h3><p>${escapeHTML(r.summary||r.description||'')}</p>${r.location?`<small>${escapeHTML(r.location)}</small>`:''}</article>`).join(''):`<div class="empty-state">No published ${escapeHTML(type)} yet.</div>`}
async function loadNews(){const rows=await q('news_articles','title,excerpt,category,published_at,slug,cover_image_url',x=>x.eq('status','published').order('published_at',{ascending:false}).limit(6));$('newsGrid').innerHTML=rows.length?rows.map(r=>`<article class="content-card">${safeUrl(r.cover_image_url)?`<img loading="lazy" src="${escapeHTML(safeUrl(r.cover_image_url))}" alt="${escapeHTML(r.title)}" style="width:100%;height:180px;object-fit:cover;border-radius:12px;margin-bottom:14px">`:''}<span class="meta">${escapeHTML(r.category||'News')}</span><h3>${escapeHTML(r.title)}</h3><p>${escapeHTML(r.excerpt||'')}</p><small>${r.published_at?new Date(r.published_at).toLocaleDateString():''}</small></article>`).join(''):'<div class="empty-state">Published news and stories will appear here.</div>'}
async function loadEvents(){const rows=await q('events','title,description,event_date,location,cover_image_url,status',x=>x.eq('status','published').order('event_date',{ascending:false}).limit(6));const el=$('eventsGrid');if(!el)return;el.innerHTML=rows.length?rows.map(r=>`<article class="story-card">${safeUrl(r.cover_image_url)?`<img loading="lazy" src="${escapeHTML(safeUrl(r.cover_image_url))}" alt="${escapeHTML(r.title)}">`:''}<div><span class="meta">${r.event_date?escapeHTML(new Date(r.event_date).toLocaleDateString()):'Event'}</span><h3>${escapeHTML(r.title)}</h3><p>${escapeHTML(r.description||r.location||'')}</p>${r.location?`<small>${escapeHTML(r.location)}</small>`:''}</div></article>`).join(''):'<div class="empty-state compact">Published events will appear here.</div>'} 
async function loadLeadership(){const rows=await q('leadership','name,title,bio,photo_url,sort_order,organisation_type',x=>x.eq('is_published',true).order('sort_order').limit(8));const sec=$('leadershipSection'),el=$('leadershipGrid');if(!sec||!el)return;if(!rows.length){sec.hidden=true;return}sec.hidden=false;el.innerHTML=rows.map(r=>`<article class="leader-card">${safeUrl(r.photo_url)?`<img loading="lazy" src="${escapeHTML(safeUrl(r.photo_url))}" alt="${escapeHTML(r.name)}">`:`<div class="leader-placeholder" aria-hidden="true">${escapeHTML((r.name||'N').slice(0,1))}</div>`}<div><span>${escapeHTML(r.organisation_type==='school'?'Nipe International School':r.organisation_type==='joint'?'Joint Leadership':'NIS Child Development Centre')}</span><h3>${escapeHTML(r.name)}</h3><strong>${escapeHTML(r.title)}</strong>${r.bio?`<p>${escapeHTML(r.bio)}</p>`:''}</div></article>`).join('')}
async function loadSupporters(){const [partners,supporters]=await Promise.all([q('partners','name,logo_url,description,category,website_url',x=>x.eq('is_published',true).eq('status','active').order('name').limit(24)),q('supporter_recognition','display_name,organisation_name,supporter_type,image_url,recognition_message,contribution_area,website_url,is_featured,sort_order',x=>x.eq('is_published',true).eq('recognition_consent',true).order('is_featured',{ascending:false}).order('sort_order').limit(24))]);const sec=$('supporters'),pl=$('partnerLogoGrid'),sg=$('supporterGrid');if(!sec||!pl||!sg)return;if(!partners.length&&!supporters.length){sec.hidden=true;return}sec.hidden=false;pl.innerHTML=partners.length?partners.map(r=>{const body=`${safeUrl(r.logo_url)?`<img loading="lazy" src="${escapeHTML(safeUrl(r.logo_url))}" alt="${escapeHTML(r.name)} logo">`:`<span>${escapeHTML(r.name)}</span>`}`;const u=safeUrl(r.website_url);return u?`<a class="partner-logo-card" href="${escapeHTML(u)}" target="_blank" rel="noopener" title="${escapeHTML(r.name)}">${body}</a>`:`<div class="partner-logo-card" title="${escapeHTML(r.name)}">${body}</div>`}).join(''):'';sg.innerHTML=supporters.length?supporters.map(r=>{const name=r.display_name||r.organisation_name||'Supporter',u=safeUrl(r.website_url);const card=`<article class="supporter-card">${safeUrl(r.image_url)?`<div class="supporter-image"><img loading="lazy" src="${escapeHTML(safeUrl(r.image_url))}" alt="${escapeHTML(name)}"></div>`:`<div class="supporter-initial" aria-hidden="true">${escapeHTML(name.slice(0,1))}</div>`}<div><span class="supporter-type">${escapeHTML(r.supporter_type||'Supporter')}</span><h3>${escapeHTML(name)}</h3>${r.organisation_name&&r.display_name?`<strong>${escapeHTML(r.organisation_name)}</strong>`:''}${r.contribution_area?`<p class="supporter-area">Supporting ${escapeHTML(r.contribution_area)}</p>`:''}${r.recognition_message?`<p>${escapeHTML(r.recognition_message)}</p>`:''}${u?`<a class="text-link dark-link" href="${escapeHTML(u)}" target="_blank" rel="noopener">Visit website →</a>`:''}</div></article>`;return card}).join(''):''}
function stopHeroRotation(){if(heroTimer){clearInterval(heroTimer);heroTimer=null}}
async function loadHeroSlides(){stopHeroRotation();heroSlides=await q('hero_slides','id,image_url,alt_text,caption,sort_order,publication_approved,child_identifiable,consent_confirmed',x=>x.eq('is_active',true).eq('publication_approved',true).order('sort_order').limit(12));heroSlides=heroSlides.filter(x=>!x.child_identifiable||x.consent_confirmed===true);if(!heroSlides.length)return;const host=$('heroMedia');if(!host)return;let img=host.querySelector('.hero-slide-image');if(!img){img=document.createElement('img');img.className='hero-slide-image';img.alt='';host.appendChild(img)}let i=0;const show=(index)=>{const slide=heroSlides[index],u=safeUrl(slide?.image_url);if(!u)return;const pre=new Image();pre.onload=()=>{img.style.opacity='0';setTimeout(()=>{img.src=u;img.alt=slide.alt_text||'NIS Child Development Centre';img.style.opacity='1'},180)};pre.src=u};show(0);if(heroSlides.length>1&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const seconds=Math.max(3,Math.min(12,Number(siteSettings.hero_slide_interval)||6));heroTimer=setInterval(()=>{if(document.hidden)return;i=(i+1)%heroSlides.length;show(i)},seconds*1000)}}
async function loadResources(){const rows=await q('documents','title,category,public_url,published_at',x=>x.eq('is_public',true).order('published_at',{ascending:false}).limit(12));$('resourceList').innerHTML=rows.length?rows.map(r=>`<a class="resource-item" href="${escapeHTML(safeUrl(r.public_url)||'#')}" target="_blank" rel="noopener"><span>📄</span><strong>${escapeHTML(r.title)}</strong><span class="badge">${escapeHTML(r.category||'Document')}</span></a>`).join(''):'<div class="empty-state">No public resources are currently available.</div>'}
async function loadGallery(){stopAlbumTimers();const albums=await q('gallery_albums','id,title,short_description,cover_image_url,slideshow_interval,category,location,event_date',x=>x.eq('status','published').eq('privacy_level','public').order('sort_order'));if(!albums.length){$('galleryGrid').innerHTML='<div class="empty-state">Published gallery albums will appear here.</div>';return}const ids=albums.map(a=>a.id),allImages=await q('gallery_media','id,album_id,image_url,caption,alt_text,sort_order,child_identifiable,consent_confirmed',x=>x.in('album_id',ids).eq('visibility','public').eq('publication_approved',true).order('sort_order'));for(const a of albums){a.images=allImages.filter(im=>String(im.album_id)===String(a.id)&&(!im.child_identifiable||im.consent_confirmed===true))}const visible=albums.filter(a=>a.images.length);$('galleryGrid').innerHTML=visible.length?visible.map(a=>{const cover=a.images.find(im=>im.image_url===a.cover_image_url)||a.images[0],img=cover?.image_url||'';return `<article class="album-card" tabindex="0" data-album="${a.id}"><div class="album-image"><img loading="lazy" src="${escapeHTML(safeUrl(img))}" alt="${escapeHTML(cover?.alt_text||a.title)}"></div><div class="album-body"><span class="album-count">${a.images.length} Photo${a.images.length===1?'':'s'}${a.category?` • ${escapeHTML(a.category)}`:''}</span><h3>${escapeHTML(a.title)}</h3><p>${escapeHTML(a.short_description||a.location||'')}</p></div></article>`}).join(''):'<div class="empty-state">Published gallery albums will appear here.</div>';visible.forEach(a=>{const card=document.querySelector(`[data-album="${a.id}"]`);card?.addEventListener('click',()=>openGallery(a));card?.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openGallery(a)}});startAlbumRotation(card,a)});window.__nisAlbums=visible}
function startAlbumRotation(card,album){if(!card||album.images.length<2||matchMedia('(prefers-reduced-motion: reduce)').matches)return;let i=0,paused=false,visible=true;const img=card.querySelector('img');const interval=Math.max(3,Math.min(12,Number(album.slideshow_interval)||5))*1000;const tick=()=>{if(paused||!visible||document.hidden)return;i=(i+1)%album.images.length;const next=safeUrl(album.images[i].image_url);if(!next)return;const pre=new Image();pre.onload=()=>{img.style.opacity='0';setTimeout(()=>{img.src=next;img.alt=album.images[i].alt_text||album.title;img.style.opacity='1'},180)};pre.onerror=()=>{};pre.src=next};const t=setInterval(tick,interval);albumTimers.set(album.id,t);if('IntersectionObserver'in window){const obs=new IntersectionObserver(entries=>{visible=entries.some(e=>e.isIntersecting&&e.intersectionRatio>.05)},{threshold:[0,.05]});obs.observe(card);albumObservers.push(obs)}card.addEventListener('mouseenter',()=>paused=true);card.addEventListener('mouseleave',()=>paused=false);card.addEventListener('focusin',()=>paused=true);card.addEventListener('focusout',()=>paused=false)}
function stopAlbumTimers(){albumTimers.forEach(clearInterval);albumTimers.clear();albumObservers.forEach(o=>o.disconnect());albumObservers=[]}
let viewer={album:null,index:0,timer:null,playing:true};function openGallery(album){viewer.album=album;viewer.index=0;viewer.playing=!matchMedia('(prefers-reduced-motion: reduce)').matches;$('galleryModal').hidden=false;document.body.style.overflow='hidden';renderViewer();resetViewerTimer();$('galleryPlayPause').focus()}
function renderViewer(){const a=viewer.album;if(!a||!a.images.length)return;const im=a.images[viewer.index];$('galleryImage').src=safeUrl(im.image_url);$('galleryImage').alt=im.alt_text||a.title;$('galleryTitle').textContent=a.title;$('galleryCaption').textContent=im.caption||a.short_description||'';$('galleryCounter').textContent=`${viewer.index+1} / ${a.images.length}`;$('galleryPlayPause').textContent=viewer.playing?'Pause slideshow':'Play slideshow';$('galleryThumbs').innerHTML=a.images.map((x,i)=>`<button class="${i===viewer.index?'active':''}" data-thumb="${i}" aria-label="View image ${i+1}"><img src="${escapeHTML(safeUrl(x.image_url))}" alt=""></button>`).join('');$('galleryThumbs').querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>{viewer.index=Number(b.dataset.thumb);renderViewer();resetViewerTimer()}))}
function viewerMove(d){if(!viewer.album?.images.length)return;viewer.index=(viewer.index+d+viewer.album.images.length)%viewer.album.images.length;renderViewer();resetViewerTimer()}function resetViewerTimer(){clearInterval(viewer.timer);if(viewer.playing&&viewer.album?.images.length>1)viewer.timer=setInterval(()=>viewerMove(1),Math.max(4,Math.min(12,Number(viewer.album.slideshow_interval)||5))*1000)}function closeGallery(){clearInterval(viewer.timer);$('galleryModal').hidden=true;document.body.style.overflow=''}
async function submitRpc(name,args,statusEl){if(!neonClient){statusEl.textContent='Online submission is unavailable until Neon is configured.';return false}statusEl.textContent='Submitting…';const {data,error}=await neonClient.rpc(name,args);if(error){statusEl.textContent='Submission could not be completed. Please try again later.';console.error(error);return false}statusEl.textContent=data?.message||'Submitted successfully.';return data||{ok:true}}
function initForms(){$('contactForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget);if(f.get('website'))return;const ok=await submitRpc('submit_contact_message_v2',{p_name:f.get('name'),p_email:f.get('email'),p_phone:f.get('phone')||null,p_category:f.get('category'),p_subject:f.get('subject')||null,p_message:f.get('message')},$('contactStatus'));if(ok)e.currentTarget.reset()});$('newsletterForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const ok=await submitRpc('subscribe_newsletter_v2',{p_name:f.get('name')||null,p_email:f.get('email')},$('newsletterStatus'));if(ok)e.currentTarget.reset()});$('donationForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget),status=$('donationStatus'),methodName=f.get('preferred_method')||'Contact Organisation',method=donationPublicConfig.methods.find(m=>m.name===methodName);const result=await submitRpc('create_donation_intent_v2',{p_name:f.get('name'),p_email:f.get('email'),p_phone:f.get('phone')||null,p_designation:f.get('designation'),p_amount:Number(f.get('amount')),p_currency:f.get('currency'),p_donation_type:f.get('donation_type')||'one_time',p_preferred_method:methodName},status);if(!result)return;if(method?.online_enabled&&method?.provider_code==='paystack'){if((f.get('donation_type')||'one_time')==='monthly'){status.textContent='Your monthly support enquiry was recorded. Recurring online billing is not enabled for this payment method yet.';e.currentTarget.reset();await loadDonationPublicConfig();return}status.textContent='Your donation reference '+(result.reference_code||'')+' has been recorded. Online card checkout is intentionally disabled in this static Neon edition because payment secret keys must never be placed in browser code. Please use the approved donation instructions shown on this page.';e.currentTarget.reset();await loadDonationPublicConfig();return}e.currentTarget.reset();await loadDonationPublicConfig()})}
const ADMIN_GROUP_LABELS={main:'Overview',website:'Website',mission:'Programs & Impact',content:'Stories & Media',people:'People & Trust',engagement:'Engagement',finance:'Donations & Finance',system:'System'};
const moduleDefs={
  overview:{label:'Dashboard',group:'main'},
  identity:{label:'Identity',special:'settings',group:'website',settingsGroup:'identity',roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  homepage:{label:'Homepage',special:'homepage',group:'website',roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  aboutAdmin:{label:'About',special:'settings',group:'website',settingsGroup:'about',roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  schoolAdmin:{label:'Nipe International School',special:'school',group:'website',roles:['super_admin','ngo_executive','school_admin']},
  interventions:{label:'What We Do',group:'mission',table:'intervention_areas',fields:['title','summary','description','sort_order','is_published'],roles:['super_admin','ngo_executive','program_manager','content_admin']},
  programs:{label:'Programs',group:'mission',table:'programs',fields:['title','summary','description','status','organisation_type','cover_image_url','is_published'],media:{cover_image_url:'Program Cover Image'},options:{status:['draft','planned','active','completed','suspended','archived'],organisation_type:['ngo','school','joint']},roles:['super_admin','ngo_executive','program_manager','content_admin']},
  projects:{label:'Projects',group:'mission',table:'projects',fields:['title','summary','description','status','location','cover_image_url','is_published'],media:{cover_image_url:'Project Cover Image'},options:{status:['draft','planned','active','completed','suspended','archived']},roles:['super_admin','ngo_executive','program_manager','content_admin']},
  campaigns:{label:'Campaigns',group:'mission',table:'campaigns',fields:['title','description','status','goal_amount','amount_raised','cover_image_url','is_published'],media:{cover_image_url:'Campaign Cover Image'},options:{status:['draft','upcoming','active','goal reached','completed','archived']},roles:['super_admin','ngo_executive','program_manager']},
  impact:{label:'Impact',group:'mission',table:'impact_statistics',fields:['label','value','year','sort_order','is_published'],roles:['super_admin','ngo_executive','program_manager','content_admin']},
  news:{label:'News',group:'content',table:'news_articles',fields:['title','excerpt','body','category','status','cover_image_url'],media:{cover_image_url:'News Cover Image'},options:{status:['draft','published','archived']},roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  events:{label:'Events',group:'content',table:'events',fields:['title','description','event_date','location','status','cover_image_url'],media:{cover_image_url:'Event Image'},options:{status:['draft','published','cancelled','archived']},roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  gallery:{label:'Gallery',special:'gallery',group:'content',roles:['super_admin','ngo_executive','communications_admin','content_admin','school_admin']},
  partners:{label:'Partners',group:'people',table:'partners',fields:['name','description','category','website_url','status','logo_url','is_published'],media:{logo_url:'Partner Logo'},options:{status:['active','inactive','ended','archived']},roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  supporters:{label:'Donors & Sponsors',group:'people',notice:'Public recognition is separate from private donor records. Publish a person or organisation only when explicit recognition consent has been confirmed.',table:'supporter_recognition',fields:['display_name','organisation_name','supporter_type','contribution_area','recognition_message','website_url','sort_order','is_featured','recognition_consent','is_published','image_url'],media:{image_url:'Supporter Photo or Logo'},options:{supporter_type:['individual donor','sponsor','corporate supporter','foundation','community supporter','in-kind supporter']},roles:['super_admin','ngo_executive','communications_admin']},
  leadership:{label:'Leadership',group:'people',table:'leadership',fields:['name','title','organisation_type','bio','photo_url','sort_order','is_published'],media:{photo_url:'Profile Photo'},options:{organisation_type:['ngo','school','joint']},roles:['super_admin','ngo_executive','content_admin']},
  documents:{label:'Documents',group:'people',table:'documents',fields:['title','category','description','public_url','is_public'],files:{public_url:'Public Document'},roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  messages:{label:'Messages',group:'engagement',special:'messages',roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  newsletter:{label:'Newsletter',group:'engagement',table:'newsletter_subscribers',custom:'newsletter',roles:['super_admin','ngo_executive','communications_admin']},
  donations:{label:'Donation Enquiries',group:'finance',special:'donations',roles:['super_admin','ngo_executive','finance_admin']},
  financeDonors:{label:'Private Donors',group:'finance',special:'donors',roles:['super_admin','ngo_executive','finance_admin']},
  financeDonations:{label:'Verified Donations',group:'finance',special:'verifiedDonations',roles:['super_admin','ngo_executive','finance_admin']},
  donationDesignations:{label:'Donation Designations',group:'finance',table:'donation_designations',fields:['name','description','sort_order','is_active'],secureDeleteRpc:'permanent_delete_donation_designation',roles:['super_admin','ngo_executive','finance_admin']},
  paymentMethods:{label:'Payment Methods',group:'finance',table:'donation_payment_methods',fields:['name','method_type','provider_code','online_enabled','public_instructions','sort_order','is_active'],options:{method_type:['manual','online'],provider_code:['','paystack']},secureDeleteRpc:'permanent_delete_payment_method',roles:['super_admin','ngo_executive','finance_admin']},
  contactSeo:{label:'Contact & SEO',special:'settings',settingsGroup:'contact',group:'system',roles:['super_admin','ngo_executive','communications_admin','content_admin']},
  mediaStorage:{label:'Media Storage',special:'mediaStorage',group:'system',roles:['super_admin']},
  users:{label:'Admin Users',group:'system',table:'admin_users',readonly:true,roles:['super_admin']}
}
const settingGroups={
 identity:{title:'Identity',intro:'Manage the NGO name, public identity and logo.',fields:[
   {key:'organisation_name',label:'NGO Name'},{key:'tagline',label:'Tagline'},{key:'registration_number',label:'Registration Number'},{key:'organisation_logo',label:'NGO Logo',type:'image'}]},
 homepage:{title:'Homepage',intro:'Manage the homepage hero without editing source code.',fields:[
   {key:'hero_eyebrow',label:'Hero Tag'},{key:'hero_title',label:'Hero Title'},{key:'hero_text',label:'Hero Description',type:'textarea'},{key:'hero_slide_interval',label:'Slideshow Interval (seconds)',inputType:'number'}]},
 about:{title:'About',intro:'Keep official organisational statements accurate and administrator-controlled.',fields:[
   {key:'about_intro',label:'Who We Are',type:'textarea'},{key:'mission',label:'Mission',type:'textarea'},{key:'vision',label:'Vision',type:'textarea'},{key:'purpose',label:'Purpose',type:'textarea'},{key:'values',label:'Core Values',type:'textarea'}]},
 contact:{title:'Contact, Map & SEO',intro:'Manage public contact information, map and social-share image.',fields:[
   {key:'address',label:'Address',type:'textarea'},{key:'phone',label:'Phone'},{key:'email',label:'Email',inputType:'email'},{key:'whatsapp',label:'WhatsApp'},{key:'map_embed_url',label:'Google Maps Embed URL'},{key:'donation_instructions',label:'Donation / Bank Instructions',type:'textarea'},{key:'seo_share_image',label:'Social Share Image (recommended 1200 × 630)',type:'image'}]}
};
function resetAdminWorkspaceScroll(){const pane=$('adminContent')||document.querySelector('.admin-content');if(pane){pane.scrollTop=0;pane.scrollLeft=0}}
function adminMessage(message,type='success'){const el=$('adminGlobalStatus');if(!el)return;el.hidden=!message;el.textContent=message||'';el.className='admin-global-status '+(type==='error'?'error':'success')}
function openAdmin(){$('adminModal').hidden=false;document.body.style.overflow='hidden';adminMessage('');checkAdminSession()}
function closeAdmin(){$('adminModal').hidden=true;document.body.style.overflow='';adminMessage('')}
async function checkAdminSession(){
 if(!neonClient){$('adminLoginStatus').textContent='Neon is not configured for this build.';return}
 const {data:{session},error:sessionError}=await neonClient.auth.getSession();
 if(sessionError){$('adminLoginStatus').textContent='The login session could not be checked. Please refresh and try again.';return}
 if(!session){$('adminLoginView').hidden=false;$('adminDashboard').hidden=true;return}
 currentUser=session.user;
 const {data,error}=await neonClient.from('admin_users').select('id,email,display_name,role,is_active').eq('id',currentUser.id).maybeSingle();
 if(error||!data?.is_active){await neonClient.auth.signOut();$('adminLoginStatus').textContent='This account is not authorised for website administration.';return}
 currentAdmin=data;$('adminLoginView').hidden=true;$('adminDashboard').hidden=false;$('adminIdentity').textContent=`Logged in as ${data.display_name||data.email} • ${String(data.role||'').replaceAll('_',' ')}`;renderAdminNav();renderAdminModule(currentModule||'overview')
}
function renderAdminNav(){
 const allowed=Object.entries(moduleDefs).filter(([k,m])=>!m.roles||m.roles.includes(currentAdmin?.role)),groups=[];
 for(const [k,m] of allowed){let g=groups.find(x=>x.key===m.group);if(!g){g={key:m.group,label:ADMIN_GROUP_LABELS[m.group]||m.group||'',items:[]};groups.push(g)}g.items.push([k,m])}
 const activeGroup=moduleDefs[currentModule]?.group||'main';
 $('adminNav').innerHTML=groups.map(g=>`<section class="admin-nav-section ${g.key===activeGroup?'open':''}" data-nav-group="${escapeHTML(g.key)}"><button type="button" class="admin-nav-group-toggle" aria-expanded="${g.key===activeGroup?'true':'false'}"><span>${escapeHTML(g.label)}</span><span aria-hidden="true">⌄</span></button><div class="admin-nav-items">${g.items.map(([k,m])=>`<button type="button" data-module="${k}">${escapeHTML(m.label)}</button>`).join('')}</div></section>`).join('');
 $('adminNav').querySelectorAll('[data-module]').forEach(b=>b.addEventListener('click',()=>renderAdminModule(b.dataset.module)));
 $('adminNav').querySelectorAll('.admin-nav-group-toggle').forEach(btn=>btn.addEventListener('click',()=>{const sec=btn.closest('.admin-nav-section'),open=!sec.classList.contains('open');$('adminNav').querySelectorAll('.admin-nav-section').forEach(x=>{x.classList.remove('open');x.querySelector('.admin-nav-group-toggle')?.setAttribute('aria-expanded','false')});if(open){sec.classList.add('open');btn.setAttribute('aria-expanded','true')}}));
}

let donationPublicConfig={designations:[],methods:[]};
const DONATION_STATUSES=['new','contacted','awaiting_payment','payment_submitted','verification_pending','verified','completed','declined','cancelled','archived'];
const DONATION_MANUAL_STATUSES=['new','contacted','awaiting_payment','payment_submitted','verification_pending','declined','cancelled','archived'];
function donationStatusLabel(v){return String(v||'new').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())}
function moneyLabel(amount,currency){const n=Number(amount);return `${escapeHTML(currency||'')} ${Number.isFinite(n)?n.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2}):escapeHTML(amount||'0')}`}
function isSuperAdmin(){return currentAdmin?.role==='super_admin'}
async function permanentDeleteRpc(rpc,id,label,onSuccess){
 if(!isSuperAdmin()){adminMessage('Permanent deletion requires a Super Administrator.','error');return false}
 const confirmation=prompt(`Permanent deletion cannot be undone. Type DELETE to permanently remove ${label}.`);if(confirmation===null)return false;if(confirmation.trim().toUpperCase()!=='DELETE'){adminMessage('Permanent deletion cancelled because DELETE was not entered.','error');return false}
 const args={p_confirmation:'DELETE'};if(rpc==='permanent_delete_contact_message')args.p_message_id=id;else if(rpc==='permanent_delete_donation_intent')args.p_intent_id=id;else if(rpc==='permanent_delete_donor')args.p_donor_id=id;else if(rpc==='permanent_delete_verified_donation')args.p_donation_id=id;else if(rpc==='permanent_delete_newsletter_subscriber')args.p_subscriber_id=id;else args.p_id=id;
 const {data,error}=await neonClient.rpc(rpc,args);if(error){adminMessage('Permanent deletion blocked: '+error.message,'error');return false}adminMessage(`${label} permanently removed.`);if(onSuccess)await onSuccess(data);return true
}
const MESSAGE_STATUSES=['new','in_review','replied','resolved','archived'];
function messageStatusLabel(v){return String(v||'new').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())}
async function renderMessagesAdmin(){
 resetAdminWorkspaceScroll();const {data,error}=await neonClient.from('contact_messages').select('*').order('created_at',{ascending:false}).limit(500);if(error){$('adminModule').innerHTML=`<div class="empty-state">Messages could not be loaded: ${escapeHTML(error.message)}</div>`;return}const rows=data||[];
 $('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Messages</h3><p>Review, respond to, resolve, archive or permanently remove enquiries according to your role.</p></div></div><div class="donation-filterbar"><label>Status <select id="messageStatusFilter"><option value="">All</option>${MESSAGE_STATUSES.map(s=>`<option value="${s}">${messageStatusLabel(s)}</option>`).join('')}</select></label><label>Search <input id="messageSearch" placeholder="Reference, name, email, subject or category"></label></div><div id="messageList"></div>`;
 const draw=()=>{const status=$('messageStatusFilter').value,needle=$('messageSearch').value.trim().toLowerCase(),filtered=rows.filter(r=>(!status||r.status===status)&&(!needle||[r.reference_code,r.name,r.email,r.phone,r.subject,r.category,r.message].some(v=>String(v||'').toLowerCase().includes(needle))));$('messageList').innerHTML=filtered.length?filtered.map(r=>`<article class="donation-record-card"><div><span class="donation-reference">${escapeHTML(r.reference_code||'Enquiry')}</span><h4>${escapeHTML(r.name||'Unnamed sender')}</h4><p>${escapeHTML(r.subject||r.category||'General enquiry')}</p><small>${escapeHTML(r.email||'')} · ${escapeHTML((r.created_at||'').slice(0,10))}</small></div><div class="donation-card-actions"><span class="status-pill">${messageStatusLabel(r.status)}</span><button class="btn btn-small btn-primary" data-message-detail="${r.id}">View Details</button></div></article>`).join(''):'<div class="empty-state">No messages match this filter.</div>';$('messageList').querySelectorAll('[data-message-detail]').forEach(b=>b.onclick=()=>showMessageDetail(rows.find(r=>String(r.id)===b.dataset.messageDetail)))};$('messageStatusFilter').onchange=draw;$('messageSearch').oninput=draw;draw()
}
async function showMessageDetail(message){
 resetAdminWorkspaceScroll();const replySubject='Re: '+(message.subject||message.category||'Your enquiry')+' '+(message.reference_code||'');const mailto=`mailto:${encodeURIComponent(message.email||'')}?subject=${encodeURIComponent(replySubject)}`;
 $('adminModule').innerHTML=`<div class="admin-toolbar"><div><span class="donation-reference">${escapeHTML(message.reference_code||'')}</span><h3>${escapeHTML(message.name||'Enquiry')}</h3><p>${escapeHTML(message.subject||message.category||'General enquiry')}</p></div><button class="btn btn-outline" id="backMessageList">Back</button></div><div class="donation-detail-grid"><section class="admin-card"><h3>Enquiry Details</h3><dl class="detail-list"><div><dt>Email</dt><dd><a href="mailto:${escapeHTML(message.email||'')}">${escapeHTML(message.email||'')}</a></dd></div><div><dt>Phone</dt><dd>${escapeHTML(message.phone||'Not provided')}</dd></div><div><dt>Category</dt><dd>${escapeHTML(message.category||'General')}</dd></div><div><dt>Subject</dt><dd>${escapeHTML(message.subject||'Not supplied')}</dd></div><div><dt>Received</dt><dd>${escapeHTML(new Date(message.created_at).toLocaleString())}</dd></div><div><dt>Status</dt><dd>${messageStatusLabel(message.status)}</dd></div></dl><div class="message-body"><strong>Message</strong><p>${escapeHTML(message.message||'')}</p></div></section><section class="admin-card"><h3>Follow-up</h3><label>Status<select id="messageStatusSelect">${MESSAGE_STATUSES.map(s=>`<option value="${s}" ${message.status===s?'selected':''}>${messageStatusLabel(s)}</option>`).join('')}</select></label><label>Internal Note<textarea id="messageAdminNote" rows="4" maxlength="2000" placeholder="Add an internal follow-up note"></textarea></label>${message.admin_notes?`<div class="admin-safety-note"><strong>Previous Notes</strong><p>${escapeHTML(message.admin_notes)}</p></div>`:''}<div class="admin-actions"><button class="btn btn-primary" id="saveMessageStatus">Save Status</button><a class="btn btn-outline" id="replyMessageEmail" href="${escapeHTML(mailto)}">Reply by Email</a></div>${isSuperAdmin()?`<div class="danger-zone"><strong>Danger Zone</strong><p>Permanent deletion cannot be undone.</p><button class="btn btn-danger" id="deleteMessagePermanent">Delete Message Permanently</button></div>`:''}</section></div>`;
 $('backMessageList').onclick=()=>renderMessagesAdmin();$('saveMessageStatus').onclick=async()=>{const note=$('messageAdminNote').value||null,{data,error}=await neonClient.rpc('set_contact_message_status',{p_message_id:message.id,p_status:$('messageStatusSelect').value,p_note:note});if(error)return adminMessage('Message update failed: '+error.message,'error');message.status=data?.status||$('messageStatusSelect').value;if(note)message.admin_notes=[message.admin_notes,note].filter(Boolean).join('\n');adminMessage('Message updated.');showMessageDetail(message)};$('replyMessageEmail').onclick=async e=>{e.preventDefault();const {error}=await neonClient.rpc('set_contact_message_status',{p_message_id:message.id,p_status:'replied',p_note:'Reply email opened by administrator.'});if(error)return adminMessage('Could not record the reply action: '+error.message,'error');message.status='replied';location.href=mailto};if($('deleteMessagePermanent'))$('deleteMessagePermanent').onclick=()=>permanentDeleteRpc('permanent_delete_contact_message',message.id,'this message',renderMessagesAdmin)
}

async function handlePaymentCallback(){const params=new URLSearchParams(location.search);if(params.get('payment')==='callback'){const status=$('donationStatus');if(status)status.textContent='This static Neon edition does not process payment callbacks in the browser. Please retain your payment reference and contact the organisation for verification.';history.replaceState({},document.title,location.pathname+'#donate');location.hash='donate'}}
function csvEscape(v){let s=String(v??'');if(/^[=+\-@\t\r]/.test(s))s="'"+s;return /[",\n]/.test(s)?'"'+s.replaceAll('"','""')+'"':s}
async function renderNewsletterAdmin(){resetAdminWorkspaceScroll();const {data,error}=await neonClient.from('newsletter_subscribers').select('*').order('created_at',{ascending:false}).limit(1000);if(error){$('adminModule').innerHTML=`<div class="empty-state">Newsletter subscribers could not be loaded: ${escapeHTML(error.message)}</div>`;return}const rows=data||[],activeCount=rows.filter(r=>r.is_active).length,inactiveCount=rows.length-activeCount;$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Newsletter</h3><p>Manage subscribers, consent state and exports.</p></div><button class="btn btn-outline" id="exportNewsletterCsv">Export Active CSV</button></div><div class="donation-metrics"><div class="admin-card metric-card"><span>Total Subscribers</span><strong>${rows.length}</strong></div><div class="admin-card metric-card"><span>Active</span><strong>${activeCount}</strong></div><div class="admin-card metric-card"><span>Unsubscribed</span><strong>${inactiveCount}</strong></div></div><div class="donation-filterbar"><label>Status<select id="newsletterFilter"><option value="">All</option><option value="active">Active</option><option value="inactive">Unsubscribed</option></select></label><label>Search<input id="newsletterSearch" placeholder="Name or email"></label></div><div id="newsletterList"></div>`;const draw=()=>{const state=$('newsletterFilter').value,qv=$('newsletterSearch').value.trim().toLowerCase(),filtered=rows.filter(r=>(!state||(state==='active'?r.is_active:!r.is_active))&&(!qv||[r.name,r.email].some(v=>String(v||'').toLowerCase().includes(qv))));$('newsletterList').innerHTML=filtered.length?filtered.map(r=>`<article class="admin-list-card"><div><h4>${escapeHTML(r.name||r.email)}</h4><p>${escapeHTML(r.email)} · ${r.is_active?'Active':'Unsubscribed'}${r.source?' · '+escapeHTML(r.source):''} · ${escapeHTML((r.created_at||'').slice(0,10))}</p></div><div class="admin-list-actions"><button class="btn btn-small btn-outline" data-news-toggle="${r.id}">${r.is_active?'Unsubscribe':'Reactivate'}</button>${isSuperAdmin()?`<button class="btn btn-small btn-danger" data-news-delete="${r.id}">Delete Permanently</button>`:''}</div></article>`).join(''):'<div class="empty-state">No subscribers match this filter.</div>';$('newsletterList').querySelectorAll('[data-news-toggle]').forEach(b=>b.onclick=async()=>{const row=rows.find(r=>String(r.id)===b.dataset.newsToggle);const {data,error}=await neonClient.rpc('set_newsletter_subscriber_state',{p_subscriber_id:row.id,p_active:!row.is_active});if(error)return adminMessage('Newsletter update failed: '+error.message,'error');return renderNewsletterAdmin()});$('newsletterList').querySelectorAll('[data-news-delete]').forEach(b=>b.onclick=()=>permanentDeleteRpc('permanent_delete_newsletter_subscriber',b.dataset.newsDelete,'this newsletter subscriber',renderNewsletterAdmin))};$('newsletterFilter').onchange=draw;$('newsletterSearch').oninput=draw;$('exportNewsletterCsv').onclick=()=>{const active=rows.filter(r=>r.is_active),csv=['Name,Email,Subscribed At'].concat(active.map(r=>[r.name,r.email,r.created_at].map(csvEscape).join(','))).join('\n'),blob=new Blob([csv],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='newsletter-active-subscribers.csv';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};draw()}
async function loadDonationPublicConfig(){
 if(!neonClient)return;
 const [designations,methods]=await Promise.all([
   q('donation_designations','name,description,sort_order',x=>x.eq('is_active',true).order('sort_order').order('name')),
   q('donation_payment_methods','name,public_instructions,method_type,provider_code,online_enabled,sort_order',x=>x.eq('is_active',true).order('sort_order').order('name'))
 ]);
 const publicMethods=(methods||[]).filter(m=>m.method_type!=='online'||m.online_enabled===true);donationPublicConfig={designations,methods:publicMethods};
 const des=$('donationDesignation');if(des&&designations.length){des.innerHTML=designations.map(d=>`<option value="${escapeHTML(d.name)}">${escapeHTML(d.name)}</option>`).join('')}
 const meth=$('donationPreferredMethod');if(meth){const data=publicMethods.length?publicMethods:[{name:'Contact Organisation'}];meth.innerHTML=data.map(m=>`<option value="${escapeHTML(m.name)}">${escapeHTML(m.name)}</option>`).join('')};const refreshDonationButton=()=>{const btn=$('donationSubmitButton'),type=document.querySelector('#donationForm [name="donation_type"]'),chosen=donationPublicConfig.methods.find(m=>m.name===meth?.value);if(btn)btn.textContent=(chosen?.online_enabled&&chosen?.provider_code==='paystack'&&type?.value!=='monthly')?'Continue to Secure Payment':'Submit Donation Enquiry'};if(meth)meth.onchange=refreshDonationButton;const donationType=document.querySelector('#donationForm [name="donation_type"]');if(donationType)donationType.onchange=refreshDonationButton;refreshDonationButton()
 const list=$('donationDesignationList');if(list&&designations.length)list.innerHTML=designations.slice(0,8).map(d=>`<li>${escapeHTML(d.name)}</li>`).join('');
}
async function renderDonationAdmin(){
 resetAdminWorkspaceScroll();
 const [{data:rows,error},{data:verified,error:verifiedError}]=await Promise.all([
  neonClient.from('donation_intents').select('*').order('created_at',{ascending:false}).limit(250),
  neonClient.from('donations').select('amount,currency,status,received_at').eq('status','verified').order('received_at',{ascending:false}).limit(1000)
 ]);
 if(error){$('adminModule').innerHTML=`<div class="empty-state">Donation enquiries could not be loaded: ${escapeHTML(error.message)}</div>`;return}
 const data=rows||[],vrows=verifiedError?[]:(verified||[]),counts={new:0,pending:0,completed:0};
 data.forEach(r=>{if(r.status==='new')counts.new++;if(['contacted','awaiting_payment','payment_submitted','verification_pending','verified'].includes(r.status))counts.pending++;if(r.status==='completed')counts.completed++});
 const totals={};vrows.forEach(r=>totals[r.currency]=(totals[r.currency]||0)+Number(r.amount||0));
 $('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Donation Enquiries</h3><p>Track each enquiry from first contact through verified donation and receipt.</p></div></div>
 <div class="donation-metrics"><div class="admin-card metric-card"><span>New</span><strong>${counts.new}</strong></div><div class="admin-card metric-card"><span>In Progress</span><strong>${counts.pending}</strong></div><div class="admin-card metric-card"><span>Completed</span><strong>${counts.completed}</strong></div><div class="admin-card metric-card"><span>Verified Total</span><strong>${Object.entries(totals).map(([c,a])=>`${escapeHTML(c)} ${a.toLocaleString()}`).join(' · ')||'—'}</strong></div></div>
 <div class="donation-filterbar"><label>Status <select id="donationStatusFilter"><option value="">All</option>${DONATION_STATUSES.map(s=>`<option value="${s}">${donationStatusLabel(s)}</option>`).join('')}</select></label><label>Search <input id="donationSearch" placeholder="Reference, donor, email or designation"></label></div>
 <div id="donationEnquiryList"></div>`;
 const draw=()=>{const status=$('donationStatusFilter').value,needle=$('donationSearch').value.trim().toLowerCase();const filtered=data.filter(r=>(!status||r.status===status)&&(!needle||[r.reference_code,r.name,r.email,r.designation,r.phone].some(v=>String(v||'').toLowerCase().includes(needle))));$('donationEnquiryList').innerHTML=filtered.length?filtered.map(r=>`<article class="donation-record-card"><div><span class="donation-reference">${escapeHTML(r.reference_code||'Pending reference')}</span><h4>${escapeHTML(r.name||'Unnamed donor')}</h4><p>${moneyLabel(r.amount,r.currency)} · ${escapeHTML(r.designation||'General')} · ${r.donation_type==='monthly'?'Monthly':'One-Time'}</p><small>${escapeHTML(r.email||'')} · ${escapeHTML((r.created_at||'').slice(0,10))}</small></div><div class="donation-card-actions"><span class="status-pill status-${escapeHTML(r.status||'new')}">${donationStatusLabel(r.status)}</span><button class="btn btn-small btn-primary" data-donation-detail="${r.id}">View Details</button></div></article>`).join(''):'<div class="empty-state">No donation enquiries match this filter.</div>'; $('donationEnquiryList').querySelectorAll('[data-donation-detail]').forEach(b=>b.onclick=()=>showDonationDetail(data.find(r=>String(r.id)===b.dataset.donationDetail)))};
 $('donationStatusFilter').onchange=draw;$('donationSearch').oninput=draw;draw();
}
async function showDonationDetail(intent){
 resetAdminWorkspaceScroll();
 const [{data:activity,error:activityError},{data:gift,error:giftError},{data:methods}]=await Promise.all([neonClient.from('donation_activity_log').select('*').eq('intent_id',intent.id).order('created_at',{ascending:false}),neonClient.from('donations').select('*').eq('intent_id',intent.id).eq('status','verified').maybeSingle(),neonClient.from('donation_payment_methods').select('*').eq('is_active',true).order('sort_order')]);
 const history=activityError?[]:(activity||[]),verifiedGift=giftError?null:gift,workflowLocked=!!verifiedGift||['verified','completed'].includes(intent.status),statusChoices=workflowLocked?[intent.status]:DONATION_MANUAL_STATUSES,instructions=siteSettings.donation_instructions||'The organisation will provide approved payment instructions.',emailBody=`Donation reference: ${intent.reference_code||''}\n\n${instructions}`,mailto=`mailto:${encodeURIComponent(intent.email||'')}?subject=${encodeURIComponent('Donation instructions '+(intent.reference_code||''))}&body=${encodeURIComponent(emailBody)}`,digits=String(intent.phone||'').replace(/\D/g,'');
 $('adminModule').innerHTML=`<div class="admin-toolbar"><div><span class="donation-reference">${escapeHTML(intent.reference_code||'')}</span><h3>${escapeHTML(intent.name)}</h3><p>Donation enquiry details and follow-up history.</p></div><button class="btn btn-outline" id="backDonationList">Back</button></div><div class="donation-detail-grid"><section class="admin-card"><h3>Enquiry</h3><dl class="detail-list"><div><dt>Amount</dt><dd>${moneyLabel(intent.amount,intent.currency)}</dd></div><div><dt>Designation</dt><dd>${escapeHTML(intent.designation||'')}</dd></div><div><dt>Donation Type</dt><dd>${intent.donation_type==='monthly'?'Monthly':'One-Time'}</dd></div><div><dt>Preferred Method</dt><dd>${escapeHTML(intent.preferred_method||'Not specified')}</dd></div><div><dt>Email</dt><dd><a href="mailto:${escapeHTML(intent.email||'')}">${escapeHTML(intent.email||'')}</a></dd></div><div><dt>Phone</dt><dd>${escapeHTML(intent.phone||'Not provided')}</dd></div><div><dt>Submitted</dt><dd>${escapeHTML(new Date(intent.created_at).toLocaleString())}</dd></div><div><dt>Status</dt><dd><span class="status-pill status-${escapeHTML(intent.status)}">${donationStatusLabel(intent.status)}</span></dd></div></dl></section><section class="admin-card"><h3>Follow-up</h3>${workflowLocked?`<div class="workflow-complete-note"><strong>Financial workflow complete</strong><p>This enquiry is linked to a verified donation. Financial status can no longer be changed through the enquiry follow-up controls.</p></div>`:`<label>Status<select id="intentStatusSelect">${statusChoices.map(s=>`<option value="${s}" ${intent.status===s?'selected':''}>${donationStatusLabel(s)}</option>`).join('')}</select></label><label>Internal Note<textarea id="intentNote" rows="4" maxlength="2000" placeholder="Record calls, messages, commitments or verification notes."></textarea></label><div class="admin-actions"><button class="btn btn-primary" id="saveIntentStatus">Save Status</button><button class="btn btn-secondary" id="markIntentContacted">Mark Contacted</button></div><div class="admin-actions"><button class="btn btn-outline" id="sendDonationInstructions">Email Payment Instructions</button>${digits?`<a class="btn btn-outline" target="_blank" rel="noopener" href="https://wa.me/${escapeHTML(digits)}?text=${encodeURIComponent('Donation '+(intent.reference_code||'')+': '+instructions)}">WhatsApp</a>`:''}</div><div class="admin-actions"><button class="btn btn-secondary" id="convertIntentDonor">Convert / Link Donor</button><button class="btn btn-danger" id="archiveIntent">Archive</button></div>`}</section></div>${verifiedGift?receiptAdminHtml(verifiedGift,intent):verificationFormHtml(methods||[],intent)}<section class="admin-card"><h3>Activity History</h3><div class="activity-timeline">${history.length?history.map(h=>`<div><strong>${escapeHTML(donationStatusLabel(String(h.action||'').replace('status:','')))}</strong><span>${escapeHTML(new Date(h.created_at).toLocaleString())}</span>${h.note?`<p>${escapeHTML(h.note)}</p>`:''}</div>`).join(''):'<p>No follow-up activity recorded yet.</p>'}</div></section>${isSuperAdmin()?`<section class="admin-card danger-zone"><strong>Danger Zone</strong><p>Permanent deletion is irreversible. If this enquiry has a verified donation, delete that verified donation first.</p><button class="btn btn-danger" id="deleteIntentPermanent">Delete Enquiry Permanently</button></section>`:''}`;
 $('backDonationList').onclick=()=>renderDonationAdmin();const updateStatus=async(status,note)=>{const {data,error}=await neonClient.rpc('set_donation_intent_status',{p_intent_id:intent.id,p_status:status,p_note:note||null});if(error){adminMessage('Status update failed: '+error.message,'error');return false}intent.status=data?.status||status;adminMessage('Donation enquiry updated.');showDonationDetail(intent);return true};if(!workflowLocked){$('saveIntentStatus').onclick=()=>updateStatus($('intentStatusSelect').value,$('intentNote').value);$('markIntentContacted').onclick=()=>updateStatus('contacted',$('intentNote').value||'Donor contacted.');$('archiveIntent').onclick=()=>{if(confirm('Archive this donation enquiry? It will remain in the financial record.'))updateStatus('archived',$('intentNote').value||'Archived by administrator.')};$('sendDonationInstructions').onclick=async()=>{if(await updateStatus('awaiting_payment','Payment instructions prepared for donor.'))location.href=mailto};$('convertIntentDonor').onclick=async()=>{const {data,error}=await neonClient.rpc('convert_donation_intent_to_donor',{p_intent_id:intent.id,p_note:$('intentNote').value||null});if(error)return adminMessage('Donor conversion failed: '+error.message,'error');intent.donor_id=data?.donor_id||intent.donor_id;adminMessage('Donor linked: '+(data?.donor_number||''));showDonationDetail(intent)}}const vf=$('verifyDonationForm');if(vf)vf.onsubmit=async e=>{e.preventDefault();const f=new FormData(vf);if(!confirm(`Verify ${intent.currency} ${intent.amount} as received? This creates the official donation and receipt record.`))return;const {data,error}=await neonClient.rpc('record_verified_donation',{p_intent_id:intent.id,p_payment_method:f.get('payment_method'),p_provider_reference:f.get('provider_reference')||null,p_received_at:f.get('received_at')?new Date(f.get('received_at')).toISOString():new Date().toISOString(),p_note:f.get('note')||null});if(error)return adminMessage('Donation verification failed: '+error.message,'error');intent.status='completed';adminMessage('Donation verified. Receipt '+(data?.receipt_number||'')+' created.');showDonationDetail(intent)};bindReceiptButtons();if($('deleteIntentPermanent'))$('deleteIntentPermanent').onclick=()=>permanentDeleteRpc('permanent_delete_donation_intent',intent.id,'this donation enquiry',renderDonationAdmin)
}
function verificationFormHtml(methods,intent){const opts=methods.length?methods:[{name:'Manual Verification'}];return `<section class="admin-card donation-verification"><h3>Verify Donation</h3><p>Use this only after funds have actually been received and checked through an approved payment channel.</p><form id="verifyDonationForm" class="admin-form"><label>Payment Method<select name="payment_method" required>${opts.map(m=>`<option value="${escapeHTML(m.name)}">${escapeHTML(m.name)}</option>`).join('')}</select></label><label>Transaction / Provider Reference<input name="provider_reference" maxlength="180"></label><label>Received Date & Time<input name="received_at" type="datetime-local"></label><label class="full">Verification Note<textarea name="note" rows="3" maxlength="2000"></textarea></label><div class="full"><button class="btn btn-primary" type="submit">Mark Payment Verified & Create Receipt</button></div></form></section>`}
function receiptAdminHtml(gift,intent){return `<section class="admin-card receipt-summary"><div class="admin-toolbar"><div><h3>Verified Donation</h3><p>Receipt ${escapeHTML(gift.receipt_number||'Pending')}</p></div><button class="btn btn-secondary" data-print-receipt="${gift.id}">Print Receipt</button></div><dl class="detail-list"><div><dt>Amount</dt><dd>${moneyLabel(gift.amount,gift.currency)}</dd></div><div><dt>Designation</dt><dd>${escapeHTML(gift.designation||'')}</dd></div><div><dt>Payment Method</dt><dd>${escapeHTML(gift.payment_method||gift.provider||'')}</dd></div><div><dt>Transaction Reference</dt><dd>${escapeHTML(gift.provider_reference||'Not supplied')}</dd></div><div><dt>Received</dt><dd>${gift.received_at?escapeHTML(new Date(gift.received_at).toLocaleString()):'—'}</dd></div><div><dt>Status</dt><dd>Verified</dd></div></dl><div class="admin-actions"><button class="btn btn-outline" data-recognition-draft="${gift.id}">Create Public Recognition Draft</button></div></section>`}
function buildReceiptMarkup(gift,donor,intent){const org=siteSettings.organisation_name||document.title||'Organisation';const logo=safeUrl(siteSettings.organisation_logo||'');return `<div class="receipt-document"><div class="receipt-head">${logo?`<img src="${escapeHTML(logo)}" alt="">`:''}<div><h1>${escapeHTML(org)}</h1><p>Official Donation Receipt</p></div></div><div class="receipt-number">${escapeHTML(gift.receipt_number||'')}</div><dl class="detail-list"><div><dt>Donor</dt><dd>${escapeHTML(donor?.anonymous_preference?'Anonymous Donor':donor?.name||intent?.name||'Donor')}</dd></div><div><dt>Amount</dt><dd>${moneyLabel(gift.amount,gift.currency)}</dd></div><div><dt>Designation</dt><dd>${escapeHTML(gift.designation||'')}</dd></div><div><dt>Payment Method</dt><dd>${escapeHTML(gift.payment_method||gift.provider||'')}</dd></div><div><dt>Transaction Reference</dt><dd>${escapeHTML(gift.provider_reference||'—')}</dd></div><div><dt>Received</dt><dd>${escapeHTML(new Date(gift.received_at||gift.verified_at||gift.created_at).toLocaleString())}</dd></div><div><dt>Donation Reference</dt><dd>${escapeHTML(intent?.reference_code||'')}</dd></div></dl><p class="receipt-foot">This receipt confirms that the donation was recorded as verified by an authorised administrator. Keep it for your records.</p></div>`}
async function printDonationReceipt(id){const {data:gift,error}=await neonClient.from('donations').select('*').eq('id',id).maybeSingle();if(error||!gift)return adminMessage('Receipt could not be loaded.','error');const [{data:donor},{data:intent}]=await Promise.all([gift.donor_id?neonClient.from('donors').select('*').eq('id',gift.donor_id).maybeSingle():Promise.resolve({data:null}),gift.intent_id?neonClient.from('donation_intents').select('*').eq('id',gift.intent_id).maybeSingle():Promise.resolve({data:null})]);let area=$('receiptPrintArea');if(!area){area=document.createElement('div');area.id='receiptPrintArea';area.className='receipt-print-area';document.body.appendChild(area)}area.innerHTML=buildReceiptMarkup(gift,donor,intent);requestAnimationFrame(()=>window.print())}
function bindReceiptButtons(){document.querySelectorAll('[data-print-receipt]').forEach(b=>b.onclick=()=>printDonationReceipt(b.dataset.printReceipt));document.querySelectorAll('[data-recognition-draft]').forEach(b=>b.onclick=async()=>{if(!confirm('Create an unpublished recognition draft? Public recognition will still require explicit consent.'))return;const {data,error}=await neonClient.rpc('create_supporter_draft_from_donation',{p_donation_id:b.dataset.recognitionDraft});if(error)return adminMessage('Recognition draft failed: '+error.message,'error');adminMessage(data?.message||'Recognition draft created.')})}
async function renderVerifiedDonationsAdmin(){resetAdminWorkspaceScroll();const {data,error}=await neonClient.from('donations').select('*').order('received_at',{ascending:false}).limit(500);if(error){$('adminModule').innerHTML=`<div class="empty-state">Verified donations could not be loaded: ${escapeHTML(error.message)}</div>`;return}const rows=(data||[]).filter(r=>r.status==='verified');$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Verified Donations</h3><p>Only donations confirmed as received appear here. Amounts from different currencies are never combined.</p></div></div>${rows.length?rows.map(r=>`<article class="donation-record-card"><div><span class="donation-reference">${escapeHTML(r.receipt_number||'Receipt pending')}</span><h4>${moneyLabel(r.amount,r.currency)}</h4><p>${escapeHTML(r.designation||'')} · ${escapeHTML(r.payment_method||r.provider||'')}</p><small>${r.received_at?escapeHTML(new Date(r.received_at).toLocaleDateString()):''}</small></div><div class="donation-card-actions"><span class="status-pill status-completed">Verified</span><button class="btn btn-small btn-primary" data-print-receipt="${r.id}">Print Receipt</button><button class="btn btn-small btn-outline" data-recognition-draft="${r.id}">Recognition Draft</button>${isSuperAdmin()?`<button class="btn btn-small btn-danger" data-delete-donation="${r.id}">Delete Permanently</button>`:''}</div></article>`).join(''):'<div class="empty-state">No verified donations yet.</div>'}`;bindReceiptButtons();$('adminModule').querySelectorAll('[data-delete-donation]').forEach(b=>b.onclick=()=>permanentDeleteRpc('permanent_delete_verified_donation',b.dataset.deleteDonation,'this verified donation',renderVerifiedDonationsAdmin))}
async function renderDonorsAdmin(){resetAdminWorkspaceScroll();const [{data:donors,error},{data:gifts,error:giftsError}]=await Promise.all([neonClient.from('donors').select('*').order('created_at',{ascending:false}).limit(500),neonClient.from('donations').select('donor_id,amount,currency,status')]);if(error){$('adminModule').innerHTML=`<div class="empty-state">Private donors could not be loaded: ${escapeHTML(error.message)}</div>`;return}const valid=giftsError?[]:(gifts||[]).filter(g=>g.status==='verified');$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Private Donors</h3><p>Confidential donor profiles. Public recognition is managed separately and requires consent.</p></div></div>${(donors||[]).length?(donors||[]).map(d=>{const ds=valid.filter(g=>g.donor_id===d.id),totals={};ds.forEach(g=>totals[g.currency]=(totals[g.currency]||0)+Number(g.amount||0));return `<article class="donation-record-card"><div><span class="donation-reference">${escapeHTML(d.donor_number||'Donor')}</span><h4>${escapeHTML(d.anonymous_preference?'Anonymous preference':d.name||'Unnamed donor')}</h4><p>${escapeHTML(d.email||'')} ${d.phone?'· '+escapeHTML(d.phone):''}</p><small>${ds.length} verified donation(s)${Object.keys(totals).length?' · '+Object.entries(totals).map(([c,a])=>`${escapeHTML(c)} ${a.toLocaleString()}`).join(' · '):''}</small></div><div class="donation-card-actions"><span class="status-pill">Private</span>${isSuperAdmin()?`<button class="btn btn-small btn-danger" data-delete-donor="${d.id}">Delete Permanently</button>`:''}</div></article>`}).join(''):'<div class="empty-state">No donor profiles yet.</div>'}`;$('adminModule').querySelectorAll('[data-delete-donor]').forEach(b=>b.onclick=()=>permanentDeleteRpc('permanent_delete_donor',b.dataset.deleteDonor,'this donor profile',renderDonorsAdmin))}

async function renderAdminModule(key){
 currentModule=key;resetAdminWorkspaceScroll();adminMessage('');const activeGroup=moduleDefs[key]?.group;$('adminNav').querySelectorAll('[data-module]').forEach(b=>b.classList.toggle('active',b.dataset.module===key));if(activeGroup){$('adminNav').querySelectorAll('.admin-nav-section').forEach(sec=>{const open=sec.dataset.navGroup===activeGroup;sec.classList.toggle('open',open);sec.querySelector('.admin-nav-group-toggle')?.setAttribute('aria-expanded',String(open))})}const m=moduleDefs[key];if(!m)return;
 if(key==='overview'){const defs=[['programs','Programs'],['projects','Projects'],['news_articles','News'],['gallery_albums','Gallery Albums'],['supporter_recognition','Public Supporters'],['contact_messages','Messages'],['donation_intents','Donation Enquiries'],['donations','Verified Donations']];const counts=[];for(const [t,label] of defs){const {count,error}=await neonClient.from(t).select('*',{count:'exact',head:true});if(error){adminMessage('Some dashboard counts could not be loaded: '+error.message,'error');counts.push([label,'—'])}else counts.push([label,count||0])}$('adminModule').innerHTML=`<div class="admin-toolbar dashboard-heading"><div><span class="mini-label">Administration</span><h3>Dashboard</h3><p>Manage the NGO website through clear sections. Use Gallery for multi-image albums and Donors & Sponsors only for public recognition that has explicit consent.</p></div></div><div class="dashboard-card-grid">${counts.map(([k,v])=>`<div class="admin-card metric-card"><span>${escapeHTML(k)}</span><strong>${escapeHTML(v)}</strong></div>`).join('')}</div><div class="admin-card admin-tip"><strong>Quick workflow</strong><p>Identity & Homepage control the public first impression. Programs & Impact explain the mission. Stories & Media keep the site current. People & Trust presents leadership, partners and approved supporter recognition.</p></div>`;return}
 if(m.special==='settings')return renderSettingsGroup(m.settingsGroup);
 if(m.special==='homepage')return renderHomepageAdmin();
 if(m.special==='school')return renderSchoolAdmin();
 if(m.special==='messages')return renderMessagesAdmin();
 if(m.custom==='newsletter')return renderNewsletterAdmin();
 if(m.special==='donations')return renderDonationAdmin();
 if(m.special==='verifiedDonations')return renderVerifiedDonationsAdmin();
 if(m.special==='donors')return renderDonorsAdmin();
 if(m.special==='gallery')return renderGalleryAdmin();
 if(m.special==='mediaStorage')return renderMediaStorageAdmin();
 return renderGenericAdmin(key,m)
}
async function readFilePrefix(file,length=16){try{return new Uint8Array(await file.slice(0,length).arrayBuffer())}catch{return new Uint8Array()}}
function bytesMatch(bytes,expected,offset=0){return expected.every((value,index)=>bytes[offset+index]===value)}
async function isAllowedImageFile(file){
 if(!file||file.size<=0||file.size>12*1024*1024)return false;const type=String(file.type||'').toLowerCase(),name=String(file.name||'');if(!/\.(jpe?g|png|webp|gif)$/i.test(name))return false;
 const bytes=await readFilePrefix(file,16);if(type==='image/jpeg')return bytesMatch(bytes,[0xff,0xd8,0xff]);if(type==='image/png')return bytesMatch(bytes,[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]);if(type==='image/gif')return bytesMatch(bytes,[0x47,0x49,0x46,0x38])&&(bytes[4]===0x37||bytes[4]===0x39)&&bytes[5]===0x61;if(type==='image/webp')return bytesMatch(bytes,[0x52,0x49,0x46,0x46])&&bytesMatch(bytes,[0x57,0x45,0x42,0x50],8);return false
}
async function isAllowedPublicDocument(file){if(!file||file.size<=0||file.size>12*1024*1024)return false;if(await isAllowedImageFile(file))return true;const bytes=await readFilePrefix(file,8);return String(file.type||'').toLowerCase()==='application/pdf'&&/\.pdf$/i.test(file.name)&&bytesMatch(bytes,[0x25,0x50,0x44,0x46,0x2d])}
function uploadPath(file,folder='uploads'){const ext=(file.name.split('.').pop()||'bin').toLowerCase().replace(/[^a-z0-9]/g,'');const clean=file.name.replace(/\.[^.]+$/,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,48)||'asset';const token=crypto.randomUUID?crypto.randomUUID():Date.now()+'-'+Math.random().toString(36).slice(2,9);return `embedded:${folder}/${new Date().getFullYear()}/${token}-${clean}.${ext}`}
function readAsDataUrl(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result||''));r.onerror=()=>reject(new Error('The selected file could not be read.'));r.readAsDataURL(file)})}
async function compressImageToDataUrl(file){
 if(String(file.type).toLowerCase()==='image/gif'){if(file.size>900*1024)throw new Error('Animated GIF files must be 900 KB or smaller.');return readAsDataUrl(file)}
 const bitmap=await createImageBitmap(file),maxSide=1600,scale=Math.min(1,maxSide/Math.max(bitmap.width,bitmap.height)),canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(bitmap.width*scale));canvas.height=Math.max(1,Math.round(bitmap.height*scale));const ctx=canvas.getContext('2d',{alpha:true});ctx.drawImage(bitmap,0,0,canvas.width,canvas.height);if(bitmap.close)bitmap.close();let quality=.84,url=canvas.toDataURL('image/webp',quality);while(url.length>1200000&&quality>.52){quality-=.08;url=canvas.toDataURL('image/webp',quality)}if(url.length>1400000)throw new Error('This image remains too large after optimisation. Choose a smaller image.');return url
}
function canvasToBlob(canvas,type='image/webp',quality=.82){return new Promise((resolve,reject)=>canvas.toBlob(b=>b?resolve(b):reject(new Error('The selected image could not be optimised.')),type,quality))}
async function compressImageToBlob(file){
 const type=String(file.type||'').toLowerCase();if(type==='image/gif')return file;
 const bitmap=await createImageBitmap(file),maxSide=1600,scale=Math.min(1,maxSide/Math.max(bitmap.width,bitmap.height)),canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(bitmap.width*scale));canvas.height=Math.max(1,Math.round(bitmap.height*scale));canvas.getContext('2d',{alpha:true}).drawImage(bitmap,0,0,canvas.width,canvas.height);if(bitmap.close)bitmap.close();let quality=.82,blob=await canvasToBlob(canvas,'image/webp',quality);while(blob.size>950*1024&&quality>.5){quality-=.08;blob=await canvasToBlob(canvas,'image/webp',quality)}if(blob.size>1500*1024)throw new Error('This image remains too large after optimisation. Choose a smaller image.');return blob
}
function mediaWorkerBase(){const raw=String(cfg.videoUploadWorkerUrl||'').trim().replace(/\/+$/,'');try{const u=new URL(raw);return u.protocol==='https:'?u.origin:''}catch{return''}}
async function adminAccessToken(){if(!neonClient)throw new Error('Neon is not configured.');const {data,error}=await neonClient.auth.getSession();if(error||!data?.session)throw new Error('Administrator session is not available. Sign in again.');return data.session.access_token||data.session.accessToken||data.session.token||''}
function cleanMediaScope(folder='uploads'){return String(folder||'uploads').toLowerCase().split('/').map(x=>x.replace(/[^a-z0-9_-]+/g,'-').replace(/^-+|-+$/g,'').slice(0,48)).filter(Boolean).slice(0,5).join('/')||'uploads'}
async function uploadImageBlobToR2(blob,name,folder='uploads'){
 const base=mediaWorkerBase();if(!base)throw new Error('Cloudflare R2 media storage is not configured.');const token=await adminAccessToken();if(!token)throw new Error('Administrator authentication token is unavailable.');const scope=cleanMediaScope(folder),filename=String(name||'image').slice(0,180);const res=await fetch(`${base}/image/upload?scope=${encodeURIComponent(scope)}&name=${encodeURIComponent(filename)}`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':blob.type||'image/webp'},body:blob});let body=null;try{body=await res.json()}catch{}if(!res.ok)throw new Error(body?.error||`R2 image upload failed with HTTP ${res.status}.`);if(!safeUrl(body?.url)||!body?.key)throw new Error('R2 did not return a valid media URL.');return{url:body.url,path:body.key}
}
function managedStoragePath(value){try{const base=mediaWorkerBase();if(!base)return'';const u=new URL(String(value||''));if(u.origin!==base||!u.pathname.startsWith('/media/'))return'';const key=u.pathname.slice('/media/'.length).split('/').map(decodeURIComponent).join('/');return key.startsWith('images/')?key:''}catch{return''}}
async function removeManagedMedia(values){const list=(Array.isArray(values)?values:[values]).filter(Boolean);if(!list.length)return;const base=mediaWorkerBase();if(!base)return;let token='';try{token=await adminAccessToken()}catch{return}for(const value of list){const key=managedStoragePath(value);if(!key)continue;try{await fetch(`${base}/image/object?key=${encodeURIComponent(key)}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}})}catch(e){console.warn('R2 image cleanup failed',e)}}}
async function uploadPublicFile(file,folder='uploads',documentMode=false){
 const allowed=documentMode?await isAllowedPublicDocument(file):await isAllowedImageFile(file);if(!allowed)throw new Error(documentMode?'Use a valid PDF, JPG, PNG, WEBP or GIF file.':'Use a valid JPG, PNG, WEBP or GIF image.');if(String(file.type).toLowerCase()==='application/pdf'){if(file.size>1000*1024)throw new Error('PDF files must be 1 MB or smaller in this edition. For larger documents, paste a public HTTPS URL instead.');const url=await readAsDataUrl(file);if(!safeUrl(url))throw new Error('The selected document could not be converted into a safe embedded asset.');return{url,path:uploadPath(file,folder)}}const blob=await compressImageToBlob(file),name=String(file.type).toLowerCase()==='image/gif'?file.name:file.name.replace(/\.[^.]+$/,'')+'.webp';return uploadImageBlobToR2(blob,name,folder)
}
function dataUrlToFile(dataUrl,name='migrated-image'){const m=String(dataUrl||'').match(/^data:(image\/(?:jpeg|png|webp|gif));base64,(.+)$/i);if(!m)throw new Error('Unsupported embedded image.');const raw=atob(m[2]),bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);const ext=({"image/jpeg":"jpg","image/png":"png","image/webp":"webp","image/gif":"gif"})[m[1].toLowerCase()]||'img';return new File([bytes],`${name}.${ext}`,{type:m[1].toLowerCase()})}
function mediaPreviewHtml(url,label='Current image'){const safe=safeUrl(url);return `<div class="media-upload-preview">${safe?`<img src="${escapeHTML(safe)}" alt="${escapeHTML(label)}">`:'No image uploaded'}</div>`}
function mediaControlHtml(name,value,label,documentMode=false){const safe=safeUrl(value);return `<div class="full media-upload" data-media-control="${escapeHTML(name)}"><strong>${escapeHTML(label)}</strong><div class="media-inline">${mediaPreviewHtml(documentMode?'':safe,label)}<div><input type="hidden" name="${escapeHTML(name)}" value="${escapeHTML(safe||'')}"><label>Choose ${documentMode?'file':'image'}<input type="file" data-media-file="${escapeHTML(name)}" accept="${documentMode?'application/pdf,image/jpeg,image/png,image/webp,image/gif':'image/jpeg,image/png,image/webp,image/gif'}"></label><div class="admin-actions"><button class="btn btn-small btn-secondary" type="button" data-clear-media="${escapeHTML(name)}">Clear</button>${safe?`<a class="btn btn-small btn-outline" href="${escapeHTML(safe)}" target="_blank" rel="noopener">Open Current</a>`:''}</div><span class="admin-field-help">${documentMode?'PDF or image up to 10 MB.':'JPG, PNG, WEBP or GIF up to 10 MB. The file is uploaded when you save.'}</span></div></div></div>`}
function bindMediaPreviews(root){root.querySelectorAll('[data-media-file]').forEach(input=>input.addEventListener('change',()=>{const file=input.files?.[0],box=input.closest('.media-upload')?.querySelector('.media-upload-preview');if(!file||!box)return;if(file.type.startsWith('image/')){const u=URL.createObjectURL(file);box.innerHTML=`<img src="${u}" alt="Selected image preview">`}else box.textContent=file.name}));root.querySelectorAll('[data-clear-media]').forEach(btn=>btn.addEventListener('click',()=>{const name=btn.dataset.clearMedia,wrap=btn.closest('.media-upload'),hidden=wrap.querySelector(`input[name="${CSS.escape(name)}"]`),file=wrap.querySelector('[data-media-file]');if(hidden)hidden.value='';if(file)file.value='';const box=wrap.querySelector('.media-upload-preview');if(box)box.textContent='No file selected';wrap.dataset.clear='true'}))}
async function saveSettingValues(form,fields){const oldValues={},uploaded=[],rows=[];let committed=false;try{for(const f of fields){oldValues[f.key]=siteSettings[f.key]||'';let value=(form.querySelector(`[data-setting="${CSS.escape(f.key)}"]`)?.value??'').trim();if(f.type==='image'){const input=form.querySelector(`[data-setting-file="${CSS.escape(f.key)}"]`);if(input?.files?.[0]){const up=await uploadPublicFile(input.files[0],`settings/${f.key}`);uploaded.push(up.url);value=up.url}if(form.querySelector(`[data-setting-clear="${CSS.escape(f.key)}"]`)?.dataset.cleared==='true')value=''}rows.push({key:f.key,value,updated_at:new Date().toISOString(),updated_by:currentUser.id})}const {error}=await neonClient.from('website_settings').upsert(rows,{onConflict:'key'});if(error)throw new Error(error.message);committed=true;for(const row of rows)if(row.value!==oldValues[row.key]&&oldValues[row.key])await removeManagedMedia(oldValues[row.key]);try{await loadAdminSettingsStrict();adminMessage('Settings saved successfully.')}catch(refreshError){adminMessage('Settings were saved, but the admin view could not refresh: '+refreshError.message,'error')}}catch(error){if(!committed)await removeManagedMedia(uploaded);adminMessage('Save failed: '+error.message,'error')}}
async function loadAdminSettingsStrict(){
 if(!neonClient)throw new Error('Neon is not configured.');const {data,error}=await neonClient.from('website_settings').select('key,value');if(error)throw new Error(error.message);siteSettings=Object.fromEntries((data||[]).map(r=>[r.key,r.value]));renderSettings();return siteSettings
}
async function loadAdminSchoolStrict(){
 if(!neonClient)throw new Error('Neon is not configured.');const {data,error}=await neonClient.from('school_profile').select('*').limit(1);if(error)throw new Error(error.message);schoolProfile=(data||[])[0]||null;renderSettings();return schoolProfile
}
async function renderSettingsGroup(group){
 const def=settingGroups[group];try{await loadAdminSettingsStrict()}catch(error){$('adminModule').innerHTML=`<div class="empty-state">${escapeHTML(def.title)} could not be loaded. ${escapeHTML(error.message)}</div>`;adminMessage('Database error while loading '+def.title+'.','error');return}$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>${escapeHTML(def.title)}</h3><p>${escapeHTML(def.intro)}</p></div><button class="btn btn-primary" id="saveSettingGroup">Save Changes</button></div><form id="settingsGroupForm" class="admin-settings-grid">${def.fields.map(f=>{const v=siteSettings[f.key]||'';if(f.type==='image')return `<div class="setting-card full"><h4>${escapeHTML(f.label)}</h4><div class="media-inline">${mediaPreviewHtml(v,f.label)}<div><label>Choose image<input type="file" data-setting-file="${escapeHTML(f.key)}" accept="image/jpeg,image/png,image/webp,image/gif"></label><input type="hidden" data-setting="${escapeHTML(f.key)}" value="${escapeHTML(v)}"><div class="admin-actions"><button class="btn btn-small btn-secondary" type="button" data-setting-clear="${escapeHTML(f.key)}">Clear Image</button>${safeUrl(v)?`<a class="btn btn-small btn-outline" href="${escapeHTML(safeUrl(v))}" target="_blank" rel="noopener">Open Current</a>`:''}</div><span class="admin-field-help">Choose JPG, PNG, WEBP or GIF. Upload occurs when you save.</span></div></div></div>`;return `<div class="setting-card ${f.type==='textarea'?'full':''}"><label>${escapeHTML(f.label)}${f.type==='textarea'?`<textarea rows="4" data-setting="${escapeHTML(f.key)}">${escapeHTML(v)}</textarea>`:`<input type="${escapeHTML(f.inputType||'text')}" data-setting="${escapeHTML(f.key)}" value="${escapeHTML(v)}">`}</label></div>`}).join('')}</form>`;
 const form=$('settingsGroupForm');form.querySelectorAll('[data-setting-file]').forEach(input=>input.addEventListener('change',()=>{const f=input.files?.[0],box=input.closest('.setting-card').querySelector('.media-upload-preview');if(f&&box){const u=URL.createObjectURL(f);box.innerHTML=`<img src="${u}" alt="Selected image preview">`}}));form.querySelectorAll('[data-setting-clear]').forEach(btn=>btn.addEventListener('click',()=>{btn.dataset.cleared='true';const card=btn.closest('.setting-card');card.querySelector('[data-setting]').value='';card.querySelector('[data-setting-file]').value='';card.querySelector('.media-upload-preview').textContent='No image uploaded'}));$('saveSettingGroup').onclick=()=>saveSettingValues(form,def.fields)
}
async function renderHomepageAdmin(){
 await renderSettingsGroup('homepage');const module=$('adminModule');if(!module)return;const {data:slides,error}=await neonClient.from('hero_slides').select('*').order('sort_order');if(error){adminMessage('Homepage settings loaded, but slideshow images could not be loaded: '+error.message,'error');return}const list=slides||[];module.insertAdjacentHTML('beforeend',`<section class="admin-form-section hero-slide-admin"><div class="admin-toolbar"><div><h3>Homepage Slideshow</h3><p>Upload several approved images. The homepage will rotate through active images automatically.</p></div></div><form id="heroSlidesUploadForm" class="admin-form"><label class="full">Choose one or multiple images<input name="files" type="file" accept="image/jpeg,image/png,image/webp,image/gif" multiple required></label><label>Alternative Text<input name="alt_text" value="NIS Child Development Centre"></label><label>Child-identifiable Content<select name="childIdentifiable"><option value="false">No</option><option value="true">Yes</option></select></label><label>Consent Confirmed<select name="consent"><option value="false">No / Not Yet</option><option value="true">Yes</option></select></label><label class="full admin-confirm"><input type="checkbox" name="publicationApproved" value="true" required> I confirm these images are approved for public website publication.</label><div class="full admin-safety-note"><strong>Safeguarding:</strong> child-identifiable homepage images cannot be activated unless consent is confirmed.</div><div class="full"><button class="btn btn-primary" type="submit">Upload Slideshow Images</button><p id="heroSlidesUploadStatus" class="form-status"></p></div></form><div class="hero-slide-admin-list">${list.length?list.map((x,i)=>`<div class="admin-list-card"><img class="thumb" src="${escapeHTML(safeUrl(x.image_url))}" alt=""><div><h4>${escapeHTML(x.alt_text||`Homepage image ${i+1}`)}</h4><p>${x.is_active?'Active':'Inactive'} • ${x.publication_approved?'Approved':'Needs review'}${x.child_identifiable?` • Child-identifiable${x.consent_confirmed?' / consent confirmed':' / consent required'}`:''} • Order ${escapeHTML(x.sort_order)}</p></div><div class="admin-list-actions"><button class="btn btn-small btn-outline" data-hero-review="${x.id}">Review</button><button class="btn btn-small btn-outline" data-hero-up="${x.id}" ${i===0?'disabled':''}>Up</button><button class="btn btn-small btn-outline" data-hero-down="${x.id}" ${i===list.length-1?'disabled':''}>Down</button><button class="btn btn-small btn-secondary" data-hero-toggle="${x.id}">${x.is_active?'Deactivate':'Activate'}</button><button class="btn btn-small btn-danger" data-hero-delete="${x.id}">Delete</button></div></div>`).join(''):'<div class="empty-state compact">No slideshow images yet. The public hero will use the branded gradient until approved images are uploaded.</div>'}</div></section>`);
 const form=$('heroSlidesUploadForm');form.onsubmit=async e=>{e.preventDefault();const files=[...form.elements.files.files].slice(0,12),status=$('heroSlidesUploadStatus');if(!files.length)return;const childIdentifiable=form.elements.childIdentifiable.value==='true',consent=form.elements.consent.value==='true',approved=form.elements.publicationApproved.checked;if(!approved){status.textContent='Confirm publication approval before uploading.';return}if(childIdentifiable&&!consent){status.textContent='Child-identifiable homepage images require confirmed consent before publication.';return}for(const file of files){if(!(await isAllowedImageFile(file))){status.textContent='Every selected file must be a valid JPG, PNG, WEBP or GIF up to 10 MB.';return}}const uploaded=[];let committed=false;try{for(let i=0;i<files.length;i++){status.textContent=`Uploading ${i+1} / ${files.length}…`;uploaded.push(await uploadPublicFile(files[i],'homepage/slides'))}const rows=uploaded.map((up,i)=>({image_url:up.url,storage_path:up.path,alt_text:String(form.elements.alt_text.value||'NIS Child Development Centre').trim().slice(0,180),sort_order:list.length+i,is_active:true,publication_approved:true,child_identifiable:childIdentifiable,consent_confirmed:consent}));const {error}=await neonClient.from('hero_slides').insert(rows);if(error)throw new Error(error.message);committed=true;status.textContent='Homepage slideshow images uploaded successfully.';await loadHeroSlides();setTimeout(()=>renderHomepageAdmin(),250)}catch(err){if(!committed)for(const up of uploaded)await removeManagedMedia(up.url);status.textContent='Upload failed: '+err.message}};
 module.querySelectorAll('[data-hero-review]').forEach(btn=>btn.onclick=()=>{const row=list.find(x=>String(x.id)===btn.dataset.heroReview);if(row)showHeroSlideReview(row)});
 module.querySelectorAll('[data-hero-up],[data-hero-down]').forEach(btn=>btn.onclick=async()=>{const id=btn.dataset.heroUp||btn.dataset.heroDown,idx=list.findIndex(x=>String(x.id)===id),delta=btn.dataset.heroUp?-1:1,next=idx+delta;if(idx<0||next<0||next>=list.length)return;const a=list[idx],b=list[next],aOrder=Number(a.sort_order)||idx,bOrder=Number(b.sort_order)||next;const ra=await neonClient.from('hero_slides').update({sort_order:bOrder,updated_at:new Date().toISOString()}).eq('id',a.id);if(ra.error)return adminMessage('Slideshow order could not be changed: '+ra.error.message,'error');const rb=await neonClient.from('hero_slides').update({sort_order:aOrder,updated_at:new Date().toISOString()}).eq('id',b.id);if(rb.error){await neonClient.from('hero_slides').update({sort_order:aOrder,updated_at:new Date().toISOString()}).eq('id',a.id);return adminMessage('Slideshow order could not be changed: '+rb.error.message,'error')}await loadHeroSlides();renderHomepageAdmin()});
 module.querySelectorAll('[data-hero-toggle]').forEach(btn=>btn.onclick=async()=>{const row=list.find(x=>String(x.id)===btn.dataset.heroToggle);if(!row)return;if(!row.is_active&&(!row.publication_approved||(row.child_identifiable&&!row.consent_confirmed)))return adminMessage('This image cannot be activated until publication approval and required consent are confirmed.','error');const {error}=await neonClient.from('hero_slides').update({is_active:!row.is_active,updated_at:new Date().toISOString()}).eq('id',row.id);if(error)return adminMessage('Slideshow image could not be updated: '+error.message,'error');await loadHeroSlides();renderHomepageAdmin()});module.querySelectorAll('[data-hero-delete]').forEach(btn=>btn.onclick=async()=>{if(!confirm('Delete this homepage slideshow image?'))return;const row=list.find(x=>String(x.id)===btn.dataset.heroDelete);const {error}=await neonClient.from('hero_slides').delete().eq('id',btn.dataset.heroDelete);if(error)return adminMessage('Delete failed: '+error.message,'error');if(row?.image_url)await removeManagedMedia(row.image_url);await loadHeroSlides();renderHomepageAdmin()})
}
function showHeroSlideReview(row){
 $('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Review Homepage Image</h3><p>Confirm publication and child-safeguarding details before activation.</p></div><button class="btn btn-outline" id="cancelHeroReview">Cancel</button></div><div class="admin-form-section"><div class="media-upload-preview"><img src="${escapeHTML(safeUrl(row.image_url))}" alt=""></div><form id="heroReviewForm" class="admin-form"><label class="full">Alternative Text<input name="alt_text" maxlength="180" value="${escapeHTML(row.alt_text||'NIS Child Development Centre')}"></label><label>Child-identifiable Content<select name="child_identifiable"><option value="false" ${!row.child_identifiable?'selected':''}>No</option><option value="true" ${row.child_identifiable?'selected':''}>Yes</option></select></label><label>Consent Confirmed<select name="consent_confirmed"><option value="false" ${!row.consent_confirmed?'selected':''}>No</option><option value="true" ${row.consent_confirmed?'selected':''}>Yes</option></select></label><label>Publication Approved<select name="publication_approved"><option value="false" ${!row.publication_approved?'selected':''}>No</option><option value="true" ${row.publication_approved?'selected':''}>Yes</option></select></label><label>Active on Homepage<select name="is_active"><option value="false" ${!row.is_active?'selected':''}>No</option><option value="true" ${row.is_active?'selected':''}>Yes</option></select></label><div class="full admin-safety-note"><strong>Safeguarding:</strong> an active image must be publication-approved. If it identifies a child, confirmed consent is also required.</div><div class="full"><button class="btn btn-primary" type="submit">Save Review</button></div></form></div>`;
 $('cancelHeroReview').onclick=()=>renderHomepageAdmin();$('heroReviewForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),child=fd.get('child_identifiable')==='true',consent=fd.get('consent_confirmed')==='true',approved=fd.get('publication_approved')==='true',active=fd.get('is_active')==='true';if(active&&(!approved||(child&&!consent)))return adminMessage('This image cannot be activated until publication approval and required consent are confirmed.','error');const {error}=await neonClient.from('hero_slides').update({alt_text:String(fd.get('alt_text')||'').trim().slice(0,180),child_identifiable:child,consent_confirmed:consent,publication_approved:approved,is_active:active,updated_at:new Date().toISOString()}).eq('id',row.id);if(error)return adminMessage('Homepage image review could not be saved: '+error.message,'error');await loadHeroSlides();renderHomepageAdmin()}
}
async function renderSchoolAdmin(){
 let s;try{await loadAdminSettingsStrict();s=(await loadAdminSchoolStrict())||{}}catch(error){$('adminModule').innerHTML=`<div class="empty-state">Nipe International School settings could not be loaded. ${escapeHTML(error.message)}</div>`;adminMessage('Database error while loading Nipe International School.','error');return}const feature=siteSettings.school_feature_image||'';$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Nipe International School</h3><p>Manage the NGO's educational institution from one simple screen.</p></div><button class="btn btn-primary" id="saveSchoolProfile">Save School</button></div><form id="schoolProfileForm" class="admin-form"><label>School Name<input name="school_name" value="${escapeHTML(s.school_name||'Nipe International School')}"></label><label>Tagline<input name="tagline" value="${escapeHTML(s.tagline||'')}"></label><label class="full">Description<textarea name="description" rows="4">${escapeHTML(s.description||siteSettings.school_description||'')}</textarea></label><label class="full">History<textarea name="history" rows="4">${escapeHTML(s.history||'')}</textarea></label><label class="full">Educational Mission<textarea name="educational_mission" rows="4">${escapeHTML(s.educational_mission||'')}</textarea></label><label>School Website<input name="website_url" type="url" value="${escapeHTML(s.website_url||siteSettings.school_website||'https://www.nipeinternationalschool.org/')}"></label><label>Admissions URL<input name="admissions_url" type="url" value="${escapeHTML(s.admissions_url||s.website_url||siteSettings.school_website||'https://www.nipeinternationalschool.org/')}"></label><label>Email<input name="contact_email" type="email" value="${escapeHTML(s.contact_email||'')}"></label><label>Phone<input name="contact_phone" value="${escapeHTML(s.contact_phone||'')}"></label><label class="full">Relationship Description<textarea name="relationship_description" rows="3">${escapeHTML(s.relationship_description||siteSettings.school_relationship||'')}</textarea></label>${mediaControlHtml('logo_url',s.logo_url||'','School Logo')}${mediaControlHtml('school_feature_image',feature,'School Feature Image')}<div class="full"><span class="admin-field-help">The School Logo is stored in the school profile. The feature image is used on the NGO public school section.</span></div></form>`;
 const form=$('schoolProfileForm');bindMediaPreviews(form);$('saveSchoolProfile').onclick=async()=>{
  const fd=new FormData(form),payload={};for(const f of ['school_name','tagline','description','history','educational_mission','website_url','admissions_url','contact_email','contact_phone','relationship_description'])payload[f]=String(fd.get(f)||'').trim();
  const oldLogo=s.logo_url||'',oldFeature=feature;let newLogo='',newFeature='',profileCommitted=false,allCommitted=false;
  try{
   for(const name of ['logo_url','school_feature_image']){const wrap=form.querySelector(`[data-media-control="${name}"]`),input=wrap.querySelector('[data-media-file]'),hidden=wrap.querySelector(`input[name="${name}"]`);let value=hidden.value;if(input.files?.[0]){const up=await uploadPublicFile(input.files[0],`school/${name}`);value=up.url;if(name==='logo_url')newLogo=up.url;else newFeature=up.url}if(wrap.dataset.clear==='true')value='';if(name==='logo_url')payload.logo_url=value;else payload.school_feature_image=value}
   const profilePayload=Object.fromEntries(Object.entries(payload).filter(([k])=>k!=='school_feature_image'));const profileResult=s.id?await neonClient.from('school_profile').update(profilePayload).eq('id',s.id):await neonClient.from('school_profile').insert(profilePayload);
   if(profileResult.error)throw new Error(profileResult.error.message);profileCommitted=true;
   const settingRows=[{key:'school_feature_image',value:payload.school_feature_image||'',updated_by:currentUser.id},{key:'school_description',value:payload.description,updated_by:currentUser.id},{key:'school_relationship',value:payload.relationship_description,updated_by:currentUser.id},{key:'school_website',value:payload.website_url,updated_by:currentUser.id}];
   const settingResult=await neonClient.from('website_settings').upsert(settingRows,{onConflict:'key'});
   if(settingResult.error){if(newFeature)await removeManagedMedia(newFeature);if(payload.logo_url!==oldLogo&&oldLogo)await removeManagedMedia(oldLogo);adminMessage('School profile saved, but related website settings could not be saved: '+settingResult.error.message,'error');try{await loadAdminSchoolStrict()}catch(refreshError){console.error('School profile refresh failed after partial save',refreshError)}return}allCommitted=true
   if(payload.logo_url!==oldLogo&&oldLogo)await removeManagedMedia(oldLogo);if(payload.school_feature_image!==oldFeature&&oldFeature)await removeManagedMedia(oldFeature);
   try{await Promise.all([loadAdminSettingsStrict(),loadAdminSchoolStrict()]);adminMessage('Nipe International School information saved successfully.');renderSchoolAdmin()}catch(refreshError){adminMessage('Nipe International School information was saved, but the admin view could not refresh: '+refreshError.message,'error')}
  }catch(error){if(!profileCommitted&&newLogo)await removeManagedMedia(newLogo);if(!allCommitted&&newFeature)await removeManagedMedia(newFeature);adminMessage((profileCommitted?'School profile was saved, but a related operation failed: ':'School save failed: ')+error.message,'error')}
 }
}
function fieldInput(f,v='',options=null){
 const label=f.replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase()),bool=f.startsWith('is_')||f.endsWith('_enabled')||f==='recognition_consent'||f==='consent'||f==='anonymous_preference',long=['description','body','summary','excerpt','bio','recognition_message','public_instructions','internal_notes'].includes(f);if(bool){const yes=v===true||String(v).toLowerCase()==='true';return `<label>${escapeHTML(label)}<select name="${f}"><option value="false" ${!yes?'selected':''}>No</option><option value="true" ${yes?'selected':''}>Yes</option></select></label>`;}if(Array.isArray(options))return `<label>${escapeHTML(label)}<select name="${f}">${options.map(o=>`<option value="${escapeHTML(o)}" ${String(v||'').toLowerCase()===String(o).toLowerCase()?'selected':''}>${escapeHTML(String(o).replace(/\b\w/g,c=>c.toUpperCase()))}</option>`).join('')}</select></label>`;if(long)return `<label class="full">${escapeHTML(label)}<textarea name="${f}" rows="4">${escapeHTML(v||'')}</textarea></label>`;const type=f.includes('date')?'date':(f.includes('url')||f==='website_url')?'url':(['sort_order','goal_amount','amount_raised','year'].includes(f)?'number':'text');const value=type==='date'&&v?String(v).slice(0,10):(v??'');return `<label>${escapeHTML(label)}<input type="${type}" name="${f}" value="${escapeHTML(value)}"></label>`
}
function recordTitle(r){return r.title||r.name||r.display_name||r.organisation_name||r.label||r.email||r.category||r.id}
function recordThumb(m,r){const field=m.media?Object.keys(m.media)[0]:null;if(!field)return'';const url=safeUrl(r[field]);return url?`<img class="thumb" src="${escapeHTML(url)}" alt="">`:`<div class="thumb placeholder">No image</div>`}
async function renderGenericAdmin(key,m){
 let query=neonClient.from(m.table).select('*');if(!['website_settings'].includes(m.table))query=query.order('created_at',{ascending:false}).limit(100);const {data:rows,error}=await query;if(error){$('adminModule').innerHTML=`<div class="empty-state">This section could not be loaded: ${escapeHTML(error.message)}</div>`;adminMessage('Database error while loading '+m.label+'.','error');return}const data=rows||[];
 $('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>${escapeHTML(m.label)}</h3><p>${m.readonly?'Review operational records.':'Create, edit and manage records with simple forms and direct uploads.'}</p></div>${m.readonly?'':`<button class="btn btn-primary" id="addRecord">Add New</button>`}</div>${m.notice?`<div class="admin-safety-note module-notice"><strong>Important:</strong> ${escapeHTML(m.notice)}</div>`:''}${data.length?data.map(r=>`<div class="admin-list-card">${recordThumb(m,r)}<div><h4>${escapeHTML(recordTitle(r))}</h4><p>${escapeHTML(String(r.status??(r.is_published===true?'Published':r.is_public===true?'Public':'')))} ${escapeHTML((r.updated_at||r.created_at||'').slice(0,10))}</p></div><div class="admin-list-actions">${m.readonly?'':`<button class="btn btn-small btn-outline" data-edit="${r.id}">Edit</button>${m.secureDeleteRpc&&isSuperAdmin()?`<button class="btn btn-small btn-danger" data-secure-del="${r.id}">Delete Permanently</button>`:m.noDelete?'':`<button class="btn btn-small btn-danger" data-del="${r.id}">Delete</button>`}`}</div></div>`).join(''):'<div class="empty-state">No records yet.</div>'}`;
 if(m.readonly)return;$('addRecord').onclick=()=>showRecordForm(m,null);$('adminModule').querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>showRecordForm(m,data.find(r=>String(r.id)===b.dataset.edit)));$('adminModule').querySelectorAll('[data-secure-del]').forEach(b=>b.onclick=()=>permanentDeleteRpc(m.secureDeleteRpc,b.dataset.secureDel,recordTitle(data.find(r=>String(r.id)===b.dataset.secureDel))||m.label,()=>renderAdminModule(key)));$('adminModule').querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this record?'))return;const row=data.find(r=>String(r.id)===b.dataset.del);const {error}=await neonClient.from(m.table).delete().eq('id',b.dataset.del);if(error)return adminMessage('Delete failed: '+error.message,'error');if(row&&m.media)await removeManagedMedia(Object.keys(m.media).map(f=>row[f]));renderAdminModule(key)})
}
function showRecordForm(m,row){resetAdminWorkspaceScroll();
 const mediaFields=new Set(Object.keys(m.media||{})),fileFields=new Set(Object.keys(m.files||{}));const normalFields=m.fields.filter(f=>!mediaFields.has(f)&&!fileFields.has(f));$('adminModule').innerHTML=`<div class="admin-toolbar"><h3>${row?'Edit':'Add'} ${escapeHTML(m.label)}</h3><button class="btn btn-outline" id="cancelRecord">Cancel</button></div><form id="recordForm" class="admin-form">${normalFields.map(f=>fieldInput(f,row?.[f],m.options?.[f])).join('')}${[...mediaFields].map(f=>mediaControlHtml(f,row?.[f]||'',m.media[f])).join('')}${[...fileFields].map(f=>mediaControlHtml(f,row?.[f]||'',m.files[f],true)).join('')}<div class="full"><button class="btn btn-primary" type="submit">Save</button></div></form>`;
 const form=$('recordForm');bindMediaPreviews(form);$('cancelRecord').onclick=()=>renderAdminModule(currentModule);form.onsubmit=async e=>{e.preventDefault();const fd=new FormData(form),obj={},newUploads=[];normalFields.forEach(f=>{let v=fd.get(f);if(f.startsWith('is_')||f.endsWith('_enabled')||f==='recognition_consent'||f==='consent'||f==='anonymous_preference')v=v==='true';if(['sort_order','goal_amount','amount_raised','year'].includes(f)&&v!=='')v=Number(v);obj[f]=v});try{for(const f of [...mediaFields,...fileFields]){const wrap=form.querySelector(`[data-media-control="${f}"]`),input=wrap.querySelector('[data-media-file]'),hidden=wrap.querySelector(`input[name="${f}"]`);let value=hidden.value;if(input.files?.[0]){const up=await uploadPublicFile(input.files[0],`${m.table}/${f}`,fileFields.has(f));newUploads.push(up.url);value=up.url}if(wrap.dataset.clear==='true')value='';obj[f]=value}obj.updated_at=new Date().toISOString();if(!row&&['programs','projects','campaigns','news_articles','events'].includes(m.table)&&!obj.slug&&obj.title)obj.slug=obj.title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+'-'+Date.now().toString(36);const res=row?await neonClient.from(m.table).update(obj).eq('id',row.id):await neonClient.from(m.table).insert(obj);if(res.error)throw new Error(res.error.message);if(row){for(const f of [...mediaFields,...fileFields])if(obj[f]!==row[f]&&row[f])await removeManagedMedia(row[f])}adminMessage(m.label+' saved successfully.');renderAdminModule(currentModule)}catch(error){await removeManagedMedia(newUploads);adminMessage('Save failed: '+error.message,'error')}}
}
async function renderGalleryAdmin(){
 const {data:albums,error}=await neonClient.from('gallery_albums').select('*').order('sort_order');if(error){adminMessage('Gallery could not be loaded: '+error.message,'error');return}$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Gallery</h3><p>Create an album, then upload one or many images into it.</p></div><button class="btn btn-primary" id="addAlbum">Create Album</button></div>${albums?.length?albums.map(a=>`<div class="admin-list-card">${safeUrl(a.cover_image_url)?`<img class="thumb" src="${escapeHTML(safeUrl(a.cover_image_url))}" alt="">`:'<div class="thumb placeholder">Album</div>'}<div><h4>${escapeHTML(a.title)}</h4><p>${escapeHTML(a.status)} • ${escapeHTML(a.category||'Gallery')}</p></div><div class="admin-list-actions"><button class="btn btn-small btn-secondary" data-manage-album="${a.id}">Manage Images</button><button class="btn btn-small btn-outline" data-edit-album="${a.id}">Edit</button></div></div>`).join(''):'<div class="empty-state">No gallery albums yet.</div>'}`;$('addAlbum').onclick=()=>showAlbumForm(null);$('adminModule').querySelectorAll('[data-edit-album]').forEach(b=>b.onclick=()=>showAlbumForm(albums.find(a=>String(a.id)===b.dataset.editAlbum)));$('adminModule').querySelectorAll('[data-manage-album]').forEach(b=>b.onclick=()=>manageAlbumImages(albums.find(a=>String(a.id)===b.dataset.manageAlbum)))
}
function showAlbumForm(a){resetAdminWorkspaceScroll();const fields=['title','short_description','description','category','location','organisation_type','status','privacy_level','slideshow_interval','sort_order','is_featured'];$('adminModule').innerHTML=`<div class="admin-toolbar"><h3>${a?'Edit':'Create'} Gallery Album</h3><button class="btn btn-outline" id="cancelAlbum">Cancel</button></div><form id="albumForm" class="admin-form">${fields.map(f=>fieldInput(f,a?.[f],({organisation_type:['ngo','school','joint','general'],status:['draft','published','hidden','archived'],privacy_level:['public','private','restricted']})[f])).join('')}<div class="full"><button class="btn btn-primary">Save Album</button></div></form>`;$('cancelAlbum').onclick=()=>renderAdminModule('gallery');$('albumForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),obj={};fields.forEach(f=>{let v=fd.get(f);if(f==='is_featured')v=v==='true';if(['slideshow_interval','sort_order'].includes(f))v=Number(v)||0;obj[f]=v});obj.slideshow_interval=Math.max(3,Math.min(12,obj.slideshow_interval||5));obj.updated_at=new Date().toISOString();obj.slug=(obj.title||'album').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+(a?'':'-'+Date.now().toString(36));const res=a?await neonClient.from('gallery_albums').update(obj).eq('id',a.id):await neonClient.from('gallery_albums').insert(obj);if(res.error)adminMessage('Album save failed: '+res.error.message,'error');else renderAdminModule('gallery')}}
async function manageAlbumImages(album){resetAdminWorkspaceScroll();
 const {data:imgs,error}=await neonClient.from('gallery_media').select('*').eq('album_id',album.id).order('sort_order');if(error)return adminMessage('Album images could not be loaded: '+error.message,'error');const list=imgs||[];$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>${escapeHTML(album.title)}</h3><p>${list.length} image(s). Select several publication-approved files to upload them together.</p></div><button class="btn btn-outline" id="backAlbums">Back</button></div><div class="admin-card"><h3>Upload Images</h3><form id="galleryUploadForm" class="admin-form"><label class="full">Choose one or multiple images<input name="files" type="file" accept="image/jpeg,image/png,image/webp,image/gif" multiple required></label><label>Default Caption<input name="caption"></label><div class="full admin-safety-note"><strong>Public gallery upload:</strong> only upload photographs approved for public website use. Child-identifiable photographs require confirmed consent.</div><label>Consent Confirmed<select name="consent"><option value="false">No / Not Yet</option><option value="true">Yes</option></select></label><label>Child-identifiable Content<select name="childIdentifiable"><option value="false">No</option><option value="true">Yes</option></select></label><div class="full"><button class="btn btn-primary">Upload Selected Images</button><p class="form-status" id="galleryUploadStatus"></p></div></form></div><div>${list.map((im,i)=>`<div class="admin-list-card"><img class="thumb" src="${escapeHTML(safeUrl(im.image_url))}" alt=""><div><h4>${escapeHTML(im.caption||`Image ${i+1}`)}</h4><p>${im.publication_approved&&im.visibility==='public'?'Public':'Private / Review'}${album.cover_image_url===im.image_url?' • Cover':''}</p></div><div class="admin-list-actions">${im.publication_approved&&im.visibility==='public'&&(!im.child_identifiable||im.consent_confirmed)?`<button class="btn btn-small btn-secondary" data-cover-img="${im.id}">Set Cover</button>`:''}<button class="btn btn-small btn-danger" data-del-img="${im.id}">Delete</button></div></div>`).join('')}</div>`;$('backAlbums').onclick=()=>renderAdminModule('gallery');$('galleryUploadForm').onsubmit=async e=>{e.preventDefault();const form=e.currentTarget,files=[...form.elements.files.files].slice(0,20),status=$('galleryUploadStatus');if(!files.length)return;const approved=true,consent=form.elements.consent.value==='true',childIdentifiable=form.elements.childIdentifiable.value==='true';if(childIdentifiable&&!consent){status.textContent='Child-identifiable images cannot be published until consent is confirmed.';return}for(const file of files){if(!(await isAllowedImageFile(file))){status.textContent='Upload stopped. Every selected file must be a valid JPG, PNG, WEBP or GIF image up to 10 MB.';return}}const uploaded=[];let committed=false;try{for(let i=0;i<files.length;i++){status.textContent=`Uploading ${i+1} / ${files.length}…`;uploaded.push(await uploadPublicFile(files[i],`gallery/${album.id}`))}const rows=uploaded.map((up,i)=>({album_id:album.id,image_url:up.url,storage_path:up.path,caption:form.elements.caption.value.trim(),alt_text:album.title,sort_order:list.length+i,publication_approved:approved,consent_confirmed:consent,child_identifiable:childIdentifiable,visibility:approved?'public':'private'}));const {error}=await neonClient.from('gallery_media').insert(rows);if(error)throw new Error(error.message);committed=true;status.textContent='Upload completed successfully.';try{await loadGallery();setTimeout(()=>manageAlbumImages(album),300)}catch(refreshError){status.textContent='Images were saved, but the gallery preview could not refresh: '+refreshError.message}}catch(err){if(!committed)for(const x of uploaded)await removeManagedMedia(x.url);status.textContent='Upload failed: '+err.message}};$('adminModule').querySelectorAll('[data-cover-img]').forEach(b=>b.onclick=async()=>{const im=list.find(x=>String(x.id)===b.dataset.coverImg);if(!im)return;const {error}=await neonClient.from('gallery_albums').update({cover_image_url:im.image_url,updated_at:new Date().toISOString()}).eq('id',album.id);if(error)return adminMessage('Cover could not be changed: '+error.message,'error');album.cover_image_url=im.image_url;await loadGallery();manageAlbumImages(album)});$('adminModule').querySelectorAll('[data-del-img]').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this image?'))return;const im=list.find(x=>String(x.id)===b.dataset.delImg),remaining=list.filter(x=>String(x.id)!==b.dataset.delImg);const {error}=await neonClient.from('gallery_media').delete().eq('id',b.dataset.delImg);if(error)return adminMessage('Delete failed: '+error.message,'error');if(im?.image_url)await removeManagedMedia(im.image_url);if(album.cover_image_url===im?.image_url){const next=remaining.find(x=>x.publication_approved&&x.visibility==='public'&&(!x.child_identifiable||x.consent_confirmed));await neonClient.from('gallery_albums').update({cover_image_url:next?.image_url||null}).eq('id',album.id);album.cover_image_url=next?.image_url||''}await loadGallery();manageAlbumImages(album)})
}

function isEmbeddedImageValue(v){return /^data:image\/(?:jpeg|png|webp|gif);base64,/i.test(String(v||''))}
async function collectEmbeddedMedia(){
 const items=[];
 const settings=await neonClient.from('website_settings').select('key,value');if(settings.error)throw new Error(settings.error.message);for(const r of settings.data||[])if(isEmbeddedImageValue(r.value))items.push({table:'website_settings',id:r.key,field:'value',value:r.value,label:`Website setting: ${r.key}`});
 const school=await neonClient.from('school_profile').select('id,logo_url').limit(1);if(school.error)throw new Error(school.error.message);for(const r of school.data||[])if(isEmbeddedImageValue(r.logo_url))items.push({table:'school_profile',id:r.id,field:'logo_url',value:r.logo_url,label:'School logo'});
 const specs=[['hero_slides','image_url','storage_path','Homepage slideshow'],['gallery_media','image_url','storage_path','Gallery image'],['gallery_albums','cover_image_url',null,'Gallery cover'],['programs','cover_image_url',null,'Program cover'],['projects','cover_image_url',null,'Project cover'],['campaigns','cover_image_url',null,'Campaign cover'],['news_articles','cover_image_url',null,'News cover'],['events','cover_image_url',null,'Event image'],['partners','logo_url',null,'Partner logo'],['supporter_recognition','image_url',null,'Supporter image'],['leadership','photo_url',null,'Leadership photo']];
 for(const [table,field,pathField,label] of specs){const res=await neonClient.from(table).select(`id,${field}${pathField?','+pathField:''}`);if(res.error)throw new Error(`${table}: ${res.error.message}`);for(const r of res.data||[])if(isEmbeddedImageValue(r[field]))items.push({table,id:r.id,field,pathField,value:r[field],label})}
 return items
}
async function updateMigratedMedia(item,newUrl,key){
 let q;if(item.table==='website_settings')q=neonClient.from('website_settings').update({value:newUrl,updated_at:new Date().toISOString(),updated_by:currentUser.id}).eq('key',item.id);else{const obj={[item.field]:newUrl,updated_at:new Date().toISOString()};if(item.pathField)obj[item.pathField]=key;q=neonClient.from(item.table).update(obj).eq('id',item.id)}const {error}=await q;if(error)throw new Error(error.message)
}
function embeddedApproxBytes(v){const s=String(v||''),comma=s.indexOf(',');return comma<0?0:Math.floor((s.length-comma-1)*.75)}
async function renderMediaStorageAdmin(){
 resetAdminWorkspaceScroll();$('adminModule').innerHTML='<div class="admin-toolbar"><div><h3>Media Storage</h3><p>Move embedded website images out of Neon and into the dedicated Cloudflare R2 media bucket.</p></div></div><div class="admin-card"><h3>Scanning current media…</h3><p id="mediaStorageSummary">Please wait.</p></div>';
 try{const base=mediaWorkerBase();if(!base)throw new Error('Cloudflare media Worker URL is not configured.');let health;try{health=await fetch(base+'/health',{cache:'no-store'}).then(r=>r.ok?r.json():null)}catch{}if(!health?.capabilities?.image)throw new Error('Cloudflare Worker v2.3.0 image capability is not active yet. Deploy the v2.3 Worker first.');const items=await collectEmbeddedMedia(),bytes=items.reduce((n,x)=>n+embeddedApproxBytes(x.value),0);$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Media Storage</h3><p>R2-backed image storage keeps Neon lightweight and improves public page performance as the media library grows.</p></div></div><div class="admin-card"><h3>Storage status</h3><p><strong>${items.length}</strong> embedded image reference(s) remain in Neon, approximately <strong>${(bytes/1024/1024).toFixed(2)} MB</strong> of encoded media.</p><p>New image uploads in this build are stored in Cloudflare R2 automatically. Existing embedded images remain safe and visible until you migrate them.</p><div class="admin-safety-note"><strong>Safe and resumable:</strong> migration updates one record at a time. If interrupted, run it again and only remaining embedded images will be processed.</div><div class="admin-actions"><button class="btn btn-primary" id="migrateMediaToR2" ${items.length?'':'disabled'}>${items.length?'Migrate Existing Images to R2':'All Images Optimized'}</button></div><p class="form-status" id="mediaMigrationStatus"></p></div>`;
 const btn=$('migrateMediaToR2');if(btn&&!btn.disabled)btn.onclick=async()=>{if(!confirm(`Migrate ${items.length} existing embedded image reference(s) to Cloudflare R2? Public URLs will be updated automatically.`))return;btn.disabled=true;const status=$('mediaMigrationStatus'),reuse=new Map();let done=0,failed=0;for(const item of items){status.textContent=`Migrating ${done+failed+1} / ${items.length}: ${item.label}…`;let up=null,isNew=false;try{if(reuse.has(item.value))up=reuse.get(item.value);else{const file=dataUrlToFile(item.value,'migrated-image');up=await uploadImageBlobToR2(file,file.name,`migration/${item.table}/${item.field}`);reuse.set(item.value,up);isNew=true}await updateMigratedMedia(item,up.url,up.path);done++}catch(e){failed++;console.error('Media migration failed',item,e);if(isNew&&up?.url){reuse.delete(item.value);await removeManagedMedia(up.url)}}}status.textContent=`Migration finished: ${done} updated${failed?`, ${failed} failed`:''}.`;await Promise.all([loadSettings(),loadSchoolProfile(),loadGallery(),loadHeroSlides()]);setTimeout(renderMediaStorageAdmin,700)}
 }catch(e){$('adminModule').innerHTML=`<div class="admin-toolbar"><div><h3>Media Storage</h3></div></div><div class="empty-state">Media Storage is not ready: ${escapeHTML(e.message)}</div>`}
}
function initAdmin(){$('adminOpen').onclick=openAdmin;document.querySelectorAll('[data-close-admin]').forEach(x=>x.onclick=closeAdmin);$('showFirstAdminSetup').onclick=()=>{$('adminLoginForm').hidden=true;$('adminSignupForm').hidden=false;$('adminSignupStatus').textContent=''};$('backToAdminLogin').onclick=()=>{$('adminSignupForm').hidden=true;$('adminLoginForm').hidden=false};$('adminSignupForm').onsubmit=async e=>{e.preventDefault();const status=$('adminSignupStatus');if(!neonClient){status.textContent='Neon is not configured.';return}const f=new FormData(e.currentTarget),email=String(f.get('email')||'').trim(),password=String(f.get('password')||''),name=String(f.get('name')||'Primary Administrator').trim();if(password.length<12){status.textContent='Use a password of at least 12 characters.';return}status.textContent='Creating Neon Auth account…';const {error}=await neonClient.auth.signUp({email,password,options:{data:{displayName:name}}});if(error){status.textContent='Account creation failed: '+(error.message||'Please verify the email and password.');return}await neonClient.auth.signOut();status.textContent='Auth account created. Now run private/01_AUTHORIZE_FIRST_ADMIN.sql in Neon SQL Editor, then return here and sign in.'};$('adminLoginForm').onsubmit=async e=>{e.preventDefault();if(!neonClient){$('adminLoginStatus').textContent='Neon is not configured.';return}$('adminLoginStatus').textContent='Signing in…';const f=new FormData(e.currentTarget),{error}=await neonClient.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});if(error){$('adminLoginStatus').textContent='Sign-in failed. Check credentials.';return}$('adminLoginStatus').textContent='';checkAdminSession()};$('adminLogout').onclick=async()=>{await neonClient.auth.signOut();currentUser=currentAdmin=null;$('adminDashboard').hidden=true;$('adminLoginView').hidden=false;if($('adminSignupForm'))$('adminSignupForm').hidden=true;if($('adminLoginForm'))$('adminLoginForm').hidden=false};if(neonClient?.auth?.onAuthStateChange)neonClient.auth.onAuthStateChange((event,session)=>{if(!session){currentUser=currentAdmin=null;$('adminDashboard').hidden=true;$('adminLoginView').hidden=false;if($('adminSignupForm'))$('adminSignupForm').hidden=true;if($('adminLoginForm'))$('adminLoginForm').hidden=false;if(!$('adminModal').hidden&&event!=='INITIAL_SESSION')$('adminLoginStatus').textContent='Your administrator session ended. Please sign in again.'}})}
function initNav(){$('navToggle').onclick=()=>{const n=$('primaryNav'),open=n.classList.toggle('open');$('navToggle').setAttribute('aria-expanded',String(open))};document.querySelectorAll('.primary-nav a').forEach(a=>a.onclick=()=>$('primaryNav').classList.remove('open'));document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');loadFeed(b.dataset.feed)});document.querySelectorAll('[data-contact-category]').forEach(a=>a.addEventListener('click',()=>{const select=document.querySelector('#contactForm select[name="category"]');if(select)select.value=a.dataset.contactCategory}))}
function initViewer(){document.querySelectorAll('[data-close-gallery]').forEach(x=>x.onclick=closeGallery);$('galleryPrev').onclick=()=>viewerMove(-1);$('galleryNext').onclick=()=>viewerMove(1);$('galleryPlayPause').onclick=()=>{viewer.playing=!viewer.playing;renderViewer();resetViewerTimer()};document.addEventListener('keydown',e=>{if(!$('galleryModal').hidden){if(e.key==='Escape')closeGallery();if(e.key==='ArrowLeft')viewerMove(-1);if(e.key==='ArrowRight')viewerMove(1);if(e.key===' '){e.preventDefault();viewer.playing=!viewer.playing;renderViewer();resetViewerTimer()}}});let sx=0;$('galleryImage').addEventListener('touchstart',e=>sx=e.touches[0].clientX,{passive:true});$('galleryImage').addEventListener('touchend',e=>{const d=e.changedTouches[0].clientX-sx;if(Math.abs(d)>50)viewerMove(d>0?-1:1)},{passive:true})}
async function boot(){initNeon();initNav();initForms();initAdmin();initViewer();$('year').textContent=new Date().getFullYear();await Promise.all([loadSettings(),loadSchoolProfile(),loadInterventions(),loadImpact(),loadFeed(),loadNews(),loadEvents(),loadResources(),loadGallery(),loadLeadership(),loadSupporters(),loadHeroSlides(),loadDonationPublicConfig()]);await handlePaymentCallback();if(queryFailures.length&&$('siteRuntimeStatus')){$('siteRuntimeStatus').hidden=false;$('siteRuntimeStatus').textContent='Some live website content could not be loaded. Please refresh the page or try again shortly.'}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot();
})();
